# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Session Protocol

AT THE START of every session, before responding to anything else, Claude Code MUST:
1. Check if .claude/pending_session_summary.json exists
2. If it exists: read it, synthesize Key Learnings from the prior session transcript, append them to ## Key Learnings in CLAUDE.md following the - YYYY-MM-DD: format, then delete the marker file
3. Only after the marker is processed (or confirmed absent) proceed with the user's request

At the END of every session, Claude Code MUST update CLAUDE.md automatically by reading from ALL of these sources — no prompt needed:

1. **DEVLOG** — Read the newest `.docx` in `C:\Users\Admin\OneDrive\_Psitta\Docs\DevLogs\` (newest by mtime, never a hardcoded filename).
2. **GIT LOG** — Run `git log --oneline -10` to capture the latest commits.
3. **KEY LEARNINGS** — Append any new rules, mistakes, or patterns discovered during the session to the `## Key Learnings` section.
4. **INFRASTRUCTURE** — Update `## Infrastructure State` with any changed values confirmed during the session.

**HISTORY RULE**: Never overwrite previous entries in `## Key Learnings` — always append. This section is an immutable log that grows over time.

**FORMAT RULE**: Each Key Learning entry must be a single line prefixed with the ISO date and a one-line description, e.g. `- 2026-04-13: <lesson>`.

## Project Overview
Psitta is a document-to-audio platform: upload a PDF/DOCX, it is parsed, chunked, tone-classified, and synthesized to natural narration via Azure Neural TTS. Flutter desktop frontend + Python FastAPI backend + PostgreSQL + Redis Streams + S3/MinIO.

## Repo Layout
```
C:/products/psitta/
├── core/backend/src/psitta/   ← FastAPI backend (Apache 2.0)
│   ├── main.py                ← App factory (create_app)
│   ├── config.py              ← Pydantic Settings
│   ├── dependencies.py        ← FastAPI DI (get_db_session, current_user, ...)
│   ├── middleware/            ← Auth0 JWT + dev bypass
│   ├── api/v1/                ← Route handlers (documents, playback, voices, tts,
│   │                             users, projects, subscriptions, auth)
│   ├── services/              ← Business logic (document, playback, audio_cache,
│   │                             subscription, audit)
│   ├── providers/             ← External integrations behind Protocol interfaces
│   │   ├── interfaces/contracts.py   ← TTSProvider, StorageProvider, VisionProvider, ...
│   │   ├── tts_azure.py / tts_edge.py / tts_elevenlabs.py / tts_router.py
│   │   ├── storage_s3.py, vision_anthropic.py, tone_rule_based.py,
│   │   └── voice_catalog_static.py
│   ├── workers/               ← document_processor.py (Redis Streams consumer)
│   ├── models/domain.py       ← Frozen dataclass domain models
│   ├── schemas/api.py         ← Pydantic strict request/response schemas
│   └── db/
│       ├── session.py         ← AsyncSession factory
│       └── migrations/versions/   ← Alembic migrations
├── apps/desktop/              ← Flutter desktop app (Windows primary)
│   └── lib/{core,data,features,widgets}/
├── extensions/                ← Proprietary add-ons (premium-tts, voice-cloning,
│                                 advanced-tone, enterprise, analytics)
├── infra/terraform/           ← AWS infrastructure
├── devops/                    ← CI scripts, deploy helpers
├── docs/                      ← PRD, API spec, ADRs, TESTING
└── docker-compose.yml         ← Local dev stack
```

## Architecture (three tiers)
```
Routes (api/v1/*.py)         Pydantic validation, HTTP concerns
     ↓
Services (services/*.py)     Business logic, orchestration, DB transactions
     ↓
Providers (providers/*.py)   External API calls behind Protocol interfaces
     ↓
External: PostgreSQL, Azure TTS, Anthropic, S3/MinIO
```

Providers are swapped via DI, which is also how the open-core boundary works: core ships Apache-licensed providers (Azure TTS, rule-based tone, static voice catalog); `extensions/` ships proprietary alternatives (ElevenLabs, LLM tone, voice cloning). See `OPEN_CORE_BOUNDARY.md` — core features live in `core/` and `apps/`; premium features live in `extensions/`.

Document processing runs in a **separate worker container** (`psitta-worker`, `python -m psitta.workers.document_processor`), not inside the API. The pipeline is Parse → Chunk → Describe (images via Anthropic) → Classify tone → Synthesize (TTS) → Finalize, with status transitions on the `documents` row driving client progress.

Audio segments are cached in `audio_segments` keyed by `(chunk_id, voice_id, speed)` — re-playing the same doc with the same voice is free, but changing speed forces re-synthesis (SSML prosody differs).

## Stack
- Python 3.12, FastAPI, SQLAlchemy async, Alembic, PostgreSQL 16
- Redis 7 Streams for the processing queue
- Flutter 3.24 desktop (Windows), Riverpod, Dio, just_audio
- Docker Compose for local dev (postgres, redis, minio, api, worker, migrate)
- Auth: Auth0 JWT middleware; dev bypass token `Bearer dev-bypass-token` resolves to dev user UUID `00000000-0000-0000-0000-000000000001`

## Critical Rules
1. **Always back up files before modifying**: `cp file.py file.py.bak` (this is an established convention in this repo — many `.bak` files already exist next to sources).
2. **Never break existing functionality** — progressive enhancement only.
3. **Use `encoding='utf-8'`** on all Python file writes.
4. **Backend runs in Docker**: package installs go in `core/backend/Dockerfile` or `pyproject.toml`, never pip-install on the host.
5. **Alembic migrations are append-only**: always create a new versioned file in `core/backend/src/psitta/db/migrations/versions/`, never edit existing ones.
6. **Enum additions**: run `ALTER TYPE enum_name ADD VALUE 'new_value'` in a raw SQL step of the migration *before* the column change that uses it — PostgreSQL won't let you use the new value in the same transaction it was added.
7. **PRs target `develop`**, not `main`. Use Conventional Commits (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `ci:`).

## Dev Commands
```bash
# Bring up the full stack (api + worker + postgres + redis + minio)
docker compose up -d

# Infra only (when running api/worker on host)
docker compose up -d postgres redis minio

# Restart backend after code changes (bind-mounted src auto-reloads, but restart
# is needed when Dockerfile / deps / startup code change)
docker compose restart api worker

# Apply migrations
docker compose exec api alembic upgrade head
# or via the dedicated profile:
docker compose --profile migrate up migrate

# Generate a new migration
docker compose exec api alembic revision -m "description"

# Logs
docker compose logs api --tail=50
docker compose logs worker --tail=50

# Postgres shell
docker compose exec postgres psql -U psitta psitta

# Backend tests / lint / types
cd core/backend && pytest
cd core/backend && pytest tests/path/to/test_file.py::test_name   # single test
pre-commit run --all-files                                          # ruff + mypy + dart
ruff check core/backend/src
mypy core/backend/src

# Flutter
cd apps/desktop && flutter run -d windows
cd apps/desktop && flutter test
cd apps/desktop && dart analyze
```

## Database
- Users table: `id` (UUID), `auth0_id`, `email`, `display_name`, `tier` (enum: free/pro/admin), `created_at`, `last_login_at`
- Core tables: `users`, `documents`, `document_chunks`, `audio_segments`, `playback_sessions`, `voice_profiles`, `audit_log`, `subscription_plans`, `user_subscriptions`
- All queries are user-scoped via `user_id` — never write a query that could return another user's rows.

## Subscription Tiers (enforced in `services/subscription_service.py`)
| Feature              | Free       | Pro         |
|----------------------|------------|-------------|
| Docs per month       | 3          | 50          |
| Max doc size (MB)    | 10         | 50          |
| TTS voices           | Edge only  | All voices  |
| Audio cache days     | 7          | 90          |
| Archived docs        | No         | Yes         |

Stripe integration is deferred — plan changes currently go through the dev override endpoint `PATCH /users/me/plan`.

## Infrastructure State
- **AWS Account**: psitta-prod (808765744063), us-east-1
- **Cognito User Pool**: `us-east-1_zdbJm5EyI` | client: `1mtmn45trougr6oqpr1afhekp4`
- **Production API**: https://api.psitta.ai
- **ECS Cluster/Service**: `psitta-cluster` / `psitta-api` (Fargate, us-east-1)
- **Azure Speech Region**: `centralus` (corrected from `eastus` on 2026-04-13)
- **Secrets Manager**: `psitta/prod/app-secrets` (16 keys — `AZURE_TTS_REGION` added 2026-04-13)
- **.env backup**: `C:\Users\Admin\OneDrive\.env.psitta.backup`
- **DB backup**: `C:\Users\Admin\OneDrive\psitta_db_backup_20260304.sql`
- **GitHub**: github.com/thkcyberai/psitta — default working branch `develop`
- **Production TTS fallback chain**: ElevenLabs → Azure (centralus) → Edge

## Security CI Coverage
GitHub Actions workflow: `.github/workflows/security.yml`

| Tool               | Type               | Trigger                  | Status        |
|--------------------|--------------------|--------------------------|---------------|
| bandit             | Python SAST        | Every push + nightly     | Added 2026-04-13 |
| flutter pub audit  | Flutter deps       | Every push + nightly     | Added 2026-04-13 |
| pip-audit          | Python deps        | Nightly 02:00 UTC        | Pre-existing  |
| safety             | Python deps        | Nightly 02:00 UTC        | Pre-existing  |
| CodeQL             | Semantic analysis  | Nightly 02:00 UTC        | Pre-existing  |
| security-gate      | Build blocker      | Every push               | Blocks on any job failure |

- bandit scans `core/backend/src/` with `-ll` (HIGH/CRITICAL fail the build); last run: 6,639 LOC, 0 HIGH/CRITICAL.
- 4 MEDIUM bandit findings reviewed and confirmed false positives (B608 ×2 parameterized queries, B104 ECS `0.0.0.0` bind, B108 ephemeral single-container cache) — no remediation needed.
- `flutter-audit` runs in `apps/desktop/`; `security-gate` `needs:` array includes `flutter-audit`.

## M7 Security Hardening Status
| # | Task                                 | Status        | Notes |
|---|--------------------------------------|---------------|-------|
| 1 | WAF — AWS Web Application Firewall   | Done          | PHP/Laravel probe blocking, rate-limit rules |
| 2 | SAST + Dependency Scanning           | Done (2026-04-13) | bandit, pip-audit, safety, flutter pub audit, CodeQL |
| 3 | Terraform IAM Cleanup                | Done (2026-04-13) | `cross_account_admin` reconciled; `auth0_domain` / `auth0_audience` removed from `terraform.tfvars` |
| 4 | Auth0 Account Cancellation           | Next session  | Cognito stable 3 weeks, zero auth errors — irreversible, needs dedicated focus |
| 5 | Rate Limiting (FastAPI middleware)   | Queued        | Per user/IP on all API endpoints |
| 6 | SOC 2 Prep                           | Queued        | After all hardening items complete |

## Milestone Roadmap
| #  | Milestone                  | Status       | Notes |
|----|----------------------------|--------------|-------|
| M1 | Foundation Infrastructure  | Done         | VPC, RDS, S3, ECS, ALB, CloudFront, Secrets Manager |
| M2 | Login Infrastructure       | Done         | Auth0 → Cognito migration complete, saves ~$240/month |
| M3 | Subscription & Stripe      | Blocked      | Waiting on EIN (LLC opened) |
| M4 | Login Screen & Account UX  | Done         | Flutter login screen with Cognito Hosted UI |
| M5 | Application Completion     | Done         | PDF Single Source of Truth architecture, highlighting, eager TTS, MSIX v1.0.3.0 |
| M6 | Windows Packaging          | Done         | MSIX v1.0.3.0 built and packaged |
| M7 | Security Hardening         | In Progress  | WAF / SAST / IAM done — Auth0 cancel + rate limiting remain |
| M8 | Marketing Launch           | Queued       | After M7 complete |

## SWH Status
Select Word Highlight (SWH) is currently **non-functional**.

- **Root cause**: SWH relies on the ElevenLabs `/with-timestamps` endpoint; the ElevenLabs quota is exhausted (100,352 credits consumed).
- Azure and Edge TTS providers return no word-level timing data, so SWH has nothing to drive highlighting while the fallback chain is running on Azure.
- Will auto-restore when the ElevenLabs monthly billing cycle resets.
- **Option A (M6/M7)** — Forced alignment via `aeneas` or `gentle`: post-synthesis, server-side, provider-agnostic, zero API credits.
- **Option B (M6)** — Azure Speech SDK `word_boundary` events: native Azure word timestamps, matches ElevenLabs schema, ~4–6 hours of work, no Flutter changes needed.

## Next Session Priorities
1. **Auth0 cancellation** — manage.auth0.com → Settings → Cancel Subscription. Irreversible; Cognito has been stable 3 weeks with zero auth errors. Requires dedicated focus.
2. **Rate limiting** — FastAPI middleware, per user/IP, on all API endpoints.
3. **SWH Option B** — Azure Speech SDK `word_boundary` timestamps (~4–6 hours), restores SWH while running on the Azure provider.

On the horizon: M3 Stripe integration (resume when EIN arrives), ElevenLabs quota auto-reset, SOC 2 prep after M7 completes.

## Execution autonomy (added 2026-06-08)

For LOW-RISK, ADDITIVE, well-specified work (feature code, tests, docs, refactors with no behavior change), execute the whole slice in ONE turn without intermediate stop gates: read any referenced files to confirm conventions, back up before editing, implement, self-verify (py_compile + ruff on touched files; run unit tests locally), stage ONLY the specific changed files (never git add .), commit with a clear conventional message, push to develop, poll CI, and report in a few lines plus the diff and run number.

STOP and ask BEFORE proceeding (read-only diagnostic, then 2-3 options with pros/cons, then wait for approval) whenever the work touches ANY of: a database migration or schema change, auth or security, secrets, data deletion, a public or irreversible action, CI or infra config, or any decision the prompt does not cover, or anything ambiguous. Diagnostic-first discipline and checkpointed halves remain mandatory for those.

Always: secrets only in terminal or GUI, never in chat; preserve everything outside the slice; approval phrases are plain ASCII.

## Key Learnings
Immutable append-only log. Never rewrite past entries — only append new ones at the bottom.

- 2026-04-13: Devlog path is `C:\Users\Admin\OneDrive\_Psitta\Docs\DevLogs\` — always use newest by mtime, never hardcode filename
- 2026-04-13: CLAUDE.md update prompts must read file first and list section headers before using update/add verbs
- 2026-04-13: Azure TTS region is `centralus` not `eastus` — confirmed via CloudWatch `tts_router.ok` logs
- 2026-04-13: Secrets exposed in chat must be rotated immediately — never paste API keys into Claude.ai chat
- 2026-04-14: Stop hooks cannot access model context — they run as plain shell commands, so model-driven synthesis must be deferred to the next SessionStart via a pending marker
- 2026-04-14: Stop hooks must never fail the session — swallow all errors, log to a hook log file, and always exit 0
- 2026-04-14: Pending marker pattern (JSON with transcript path + git log + devlog info) is how mechanical hook work hands off to deferred model synthesis at SessionStart
- 2026-04-14: Split Session Protocol into mechanical (hook-written deterministic sections) vs model (deferred Key Learnings) — never ask a shell hook to synthesize lessons
- 2026-04-14: Use env vars (e.g. PSITTA_DEVLOG_DIR) in Stop hooks for runtime paths instead of hardcoding, so hooks remain portable across machines
- 2026-04-14: Stop hook scripts must force UTF-8 (LC_ALL=C.UTF-8, PYTHONIOENCODING=utf-8) to prevent em-dash mojibake (â€—) in git log on Windows
- 2026-04-14: Infra state updater must use first-token matching (not substring equality) so hand-written audit context like "centralus (corrected from eastus on 2026-04-13)" is preserved on re-run
- 2026-04-14: Per-tier rate-limit route matchers must be ordered longest-first — chunk matcher `/documents/{id}/chunks/{cid}/resynthesize` must precede `/documents/{id}/resynthesize` or the shorter pattern shadows it
- 2026-04-14: Authenticated routes pay a double JWT decode (rate-limit middleware + get_current_user) — mitigate with per-token cache + JWKS cache (1hr TTL); full elimination requires refactoring middleware/auth.py
- 2026-04-14: Rate limiter must fail-open — catch all exceptions, log, allow the request through; a limiter bug must never DoS the API
- 2026-04-14: RateLimitMiddleware is outermost so 429 responses bypass CORS/RequestID middleware — fine for Flutter desktop, will need reorder when a web UI ships
- 2026-04-14: Kinesis Firehose → S3 CloudWatch log objects are doubly-gzipped (outer Firehose layer + inner per-record gzip members) — a single gunzip leaves 0x1f8b markers; decoder must iterate nested gzip members
- 2026-04-15: SOC 2 CC7.2 audit_log tamper-evidence requires THREE layers — (1) REVOKE UPDATE/DELETE from app role, (2) BEFORE UPDATE OR DELETE FOR EACH ROW trigger, (3) BEFORE TRUNCATE FOR EACH STATEMENT trigger. Row-level triggers do NOT fire on TRUNCATE, and TRUNCATE is not a grantable privilege, so the statement-level trigger is the only portable guard
- 2026-04-15: For full SOC 2 audit_log tamper-evidence the table owner must be a separate migration role (e.g. `psitta_migrator`), not the app role (`psitta`) — owners can `ALTER TABLE … DISABLE TRIGGER` and bypass the guard. Out of scope for migration 011; follow-up work for the SOC 2 evidence package
- 2026-04-15: `tests/conftest.py` `test_settings` fixture uses `DATABASE_HOST`/`DATABASE_NAME`/`DATABASE_USER`/`DATABASE_PASSWORD` but `config.py` Settings defines `POSTGRES_HOST`/`POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD` — the fixture will raise on strict instantiation. Pre-existing bug, separate task to reconcile; new tests should use `get_settings()` (env-loaded) to sidestep it
- 2026-04-15: TRUNCATE does not fire row-level triggers in PostgreSQL — a separate BEFORE TRUNCATE FOR EACH STATEMENT trigger is required for full tamper-evidence
- 2026-04-15: audit_log owner-role separation needed for SOC 2: psitta_migrator owns the table, psitta app role is non-owner — prevents ALTER TABLE DISABLE TRIGGER bypass
- 2026-04-15: conftest.py uses DATABASE_HOST/DATABASE_NAME but config.py defines POSTGRES_HOST/POSTGRES_DB — pre-existing mismatch, fix separately
- 2026-04-15: CLAUDE.md SessionStart hook processes pending markers automatically on startup — no human prompt needed from this session forward
- 2026-04-15 (PM): Cloudflare email auth records (DKIM CNAME, SPF/DMARC TXT) must be DNS only (gray cloud) — proxied CNAMEs silently break DKIM signature validation
- 2026-04-15 (PM): Hostinger email DNS sequence: domain-verification TXT → MX records → SPF TXT → 3 DKIM CNAMEs → DMARC TXT on _dmarc — all configurable in Cloudflare in a single session
- 2026-04-15 (PM): Stripe Managed Payments (Merchant of Record) costs 5% + $0.50 vs standard 2.9% + $0.30 — avoid for US-focused SaaS with margin pressure; add Stripe Tax (~0.5%) later as separate product when compliance becomes real
- 2026-04-15 (PM): SaaS tax code (txcd_10103000) is correct for desktop clients backed by cloud services — Downloadable Software (txcd_10101000) is wrong classification because the product stops working if backend shuts down
- 2026-04-15 (PM): Stripe lookup keys (e.g. reading_nook_pro_monthly) let the backend reference prices semantically instead of hardcoding opaque IDs — enables future price swaps without code changes
- 2026-04-15 (PM): Creativity Nook Pro as separate Stripe product (not add-on) enables clean subscription swaps with automatic proration — customer pays $19.99/mo total, not $14.99 + $19.99
- 2026-04-15 (PM): Stripe sandbox separates account creation from business verification — EIN + bank account are requested only on Switch to live account, allowing full integration testing before legal/financial setup
- 2026-04-15 (PM): Security discipline for secrets files: never echo/cat/print values to terminal; use Claude Code prompts that explicitly prohibit printing and require user to paste via editor (Notepad/VS Code) directly into the file
- 2026-04-16: SessionStart hook protocol-based approach fails because user instructions always override model's Session Protocol rules — deterministic Python synthesis eliminates the model from the critical path entirely
- 2026-04-16: When diagnosing hook failures, check the actual log path (.claude/hooks/session_start_hook.log) not the assumed path (.claude/session_start_hook.log) — path mismatches cause false 'hook never ran' conclusions
- 2026-04-16: Marker accumulation across sessions: the Stop hook appends to pending_session_summary.json on every session end — if the Start hook never consumes, the marker grows indefinitely with stale entries from old devlogs
- 2026-04-16: Process only the newest devlog from the marker array — older entries are stale snapshots superseded by newer devlogs that already contain all previous content
- 2026-04-16: CLAUDE.md Key Learnings uses bullet format (- DATE: LEARNING) not markdown table format — the hook must read and write the actual format to avoid corrupting existing entries
- 2026-04-16: ECS --force-new-deployment re-pulls the :latest image tag from ECR without creating a new task definition revision — task definition number stays the same but the container runs new code
- 2026-04-16: Production Alembic migrations can be run via one-off ECS task with command override when the RDS is in a private subnet unreachable from the developer laptop — no SSH tunnel or security group changes needed
- 2026-04-16: RDS automated backups run daily with continuous transaction logs every 5 minutes — point-in-time recovery within the retention window (default 7 days) requires no manual setup
- 2026-04-16: Stripe webhook endpoint must always return HTTP 200 after storing the event, even on handler errors — returning 500 causes Stripe to retry for up to 3 days, flooding the server
- 2026-04-16: Insert-first webhook pattern: store the raw event payload in subscription_events BEFORE running the handler — if the handler crashes, the event is preserved for manual reprocessing
- 2026-04-16: Stripe Checkout (prebuilt hosted page) is the correct integration for Flutter desktop — flutter_stripe SDK doesn't support desktop, so browser handoff via url_launcher is the clean path
- 2026-04-16: Stripe lookup keys (e.g. reading_nook_pro_monthly) resolve price IDs at runtime via stripe.Price.list() — code references semantic names, not opaque Stripe IDs, enabling price changes without code deployment
- 2026-04-16: Plan enforcement should be a FastAPI dependency (Depends), not global middleware — only some endpoints need gating, and billing endpoints must never be gated (chicken-and-egg)
- 2026-04-16: Stripe secrets script must use getpass (no echo) and subprocess with capture_output — never echo, cat, or print key values to terminal or command history
- 2026-04-16: ECS containers are stateless — all persistent data lives in RDS, S3, and Secrets Manager. Replacing a container loses nothing. This is why --force-new-deployment is safe.
- 2026-04-16: Docker Desktop is not used for local development but Docker images are still built by GitHub Actions for ECS Fargate deployment — these are separate concerns (local dev vs production deployment)
- 2026-04-16: Local .env POSTGRES_HOST=localhost is stale from Docker era — local backend cannot connect to RDS (private subnet). All migrations and testing must go through ECS or a future bastion host
- 2026-04-16: RDS Multi-AZ should be enabled before taking real subscription payments — single-AZ means a datacenter outage takes down the entire billing system. Queued as M9 pre-launch item.
- 2026-04-17: WebView2 stores cookies in a per-process default user data folder — clearing cookies on a throwaway WebviewController invalidates the Cognito session cookie globally, forcing credential re-entry on next login
- 2026-04-17: User-scoped SharedPreferences keys (user_{userId}_key pattern) prevent preference leakage between accounts on the same device — the pattern applies to any desktop app with multi-account support
- 2026-04-17: Legacy preference migration: copy unscoped key to scoped key for the first user who logs in, then delete the legacy key immediately — second user cannot inherit first user's data
- 2026-04-17: On logout, reset Riverpod providers to defaults but do NOT delete SharedPreferences keys — user's preferences must survive across logout/login cycles. Deletion is the wrong approach; scoping is the right one.
- 2026-04-17: Player bar state (document ID, position) should be saved to user-scoped SharedPreferences before logout and restored on re-login — don't auto-play, just show the document title and position
- 2026-04-17: stay_signed_in must remain device-scoped (not user-scoped) because it governs auto-login behavior at the machine level, regardless of which account is active
- 2026-04-17: Stripe webhook URL must include the full FastAPI router prefix (/api/v1/billing/webhook, not /billing/webhook) — the prefix is defined in main.py app.include_router(prefix='/api/v1') and is easy to miss
- 2026-04-17: stripe.StripeObject does not support .get() — always convert event data to plain dict via json.loads(str(obj)) at the webhook entry point before passing to handlers
- 2026-04-17: audit_log.resource_id is UUID type — never pass Stripe IDs (sub_xxx, cus_xxx) as resource_id. Pass the Psitta user_id (UUID) and put Stripe IDs in details_json
- 2026-04-17: Webhook handler must isolate subscription_events INSERT in an independent database session that commits before the handler runs — if the handler crashes, the forensic event trail survives for manual reprocessing
- 2026-04-17: ON CONFLICT (stripe_event_id) DO NOTHING RETURNING id is the correct atomic idempotency pattern — race-free under concurrent Stripe retries, and RETURNING lets you distinguish 'just inserted' from 'duplicate'
- 2026-04-17: When Settings page and Change Plan screen disagree on the current plan, they are hitting different backend endpoints — always consolidate to a single provider and single endpoint (billingStatusProvider → GET /billing/status)
- 2026-04-17: Legacy API endpoints should be deprecated (not maintained in parallel) once a new endpoint replaces their function — maintaining two sources of truth guarantees they will diverge
- 2026-04-17: Stripe Checkout redirects to success_url after payment — if psitta.ai/billing/success doesn't exist yet, the redirect shows a DNS error page. This is cosmetic (webhook still fires), but confusing for users — build the landing page before go-live
- 2026-04-17: The 000000 code in Stripe sandbox Link verification is printed on screen ('Enter 000000 to continue') — no real SMS is sent in test mode
- 2026-04-17: Creative Nook (not Creativity Nook) is the grammatically correct English name — 'creative' is an adjective, 'creativity' is a noun, and the adjective form parallels 'reading' in Reading Nook
- 2026-04-18: SessionStart hook must scan the DevLogs folder by mtime, not rely solely on the Stop hook's marker — the devlog is created after Claude Code exits, so the marker always points to the previous day's file
- 2026-04-18: Plan enforcement should be a Flutter-side concern using the same billingStatusProvider that drives the Change Plan screen — no need for separate backend middleware when the client can gate UI features directly
- 2026-04-18: Free-to-Pro feature gating pattern: isProUser = (plan != 'free' && status == 'active'). Use this single boolean across all UI gates for consistency
- 2026-04-18: Speed preference clamp on downgrade: an app-level Riverpod listener watches the plan and auto-clamps saved speed to kFreeMaxSpeed if the user is no longer Pro — prevents stale Pro-era settings from persisting
- 2026-04-18: SWH force-off on downgrade: same pattern as speed clamp — app-level listener sets SwhMode.never when plan reverts to free, preventing word highlighting from persisting after subscription ends
- 2026-04-18: Upload limit enforcement: count documents client-side from the library list filtered by created_at in the current month — avoid a separate backend round-trip when the data is already loaded
- 2026-04-18: Hiding menu items (download, archive) is better UX than showing them grayed out for free users — grayed items create frustration, hidden items keep the interface clean
- 2026-04-18: Stripe subscription cancellation can create duplicate customer.subscription.deleted webhook events if multiple subscriptions exist for the same customer — handler must process each independently by stripe_subscription_id
- 2026-04-18: Stripe declined card test (4000 0000 0000 9995) does NOT fire a webhook event — the payment fails at checkout before a subscription is created, so no invoice.payment_failed is sent
- 2026-04-18: psitta.ai domain is registered on AWS Route 53 in the Blowmymind management account (not psitta-prod) — registered March 14, 2026, expires March 14, 2028, auto-renew on
- 2026-04-18: .ai domains do not support WHOIS privacy protection — registrant contact information is always publicly visible
- 2026-04-18: For SaaS product websites, S3 + CloudFront is the correct AWS-native hosting when the domain is already on Route 53 — avoids DNS migration to Cloudflare and keeps all infrastructure in one console
- 2026-04-18: PostHog is preferred over Google Analytics 4 for SaaS products because it includes session replay, heatmaps, and advanced funnels in the free tier — GA4 requires separate paid tools for these capabilities
- 2026-04-18: Claude Code permissions can be broadly scoped via .claude/settings.local.json with 'Read **' and 'Write **' patterns to eliminate all permission prompts within the project directory
- 2026-04-19: Claude Code permission DSL requires `:*` only at the end of a Bash(...) rule. Any character after `:*` causes silent skip at startup. Use Form A (exact), Form B (prefix with trailing :*), or Form C (bash wildcards without :*) — never mix.
- 2026-04-19: Malformed Claude Code permission rules fail silently with only a yellow “Settings Warning” banner. Run `/doctor` to surface all invalid rules, and use a Node one-liner to audit programmatically against engine grammar.
- 2026-04-19: settings.json is the shared baseline (committed); settings.local.json is per-machine (must be gitignored). Claude Code may auto-inject blanket entries into settings.local.json when the operator approves “Yes, and don’t ask again” — periodically reset to empty template to restore discipline.
- 2026-04-19: After any .claude/settings.json change, Claude Code requires a full process restart (close terminal, `claude` again) — no dynamic reload.
- 2026-04-19: .gitignore order matters. A negation rule `!pattern` on a later line overrides an ignore rule on an earlier line. Always verify with `git check-ignore -v` rather than assuming a literal-match grep is sufficient.
- 2026-04-19: AWS Organizations consolidated billing is automatic across every account in an org. Billing is never a reason to collapse workloads into one account — clean separation does not cost extra.
- 2026-04-19: Wildcard ACM cert for `*.psitta.ai` does NOT cover bare `psitta.ai` apex. Always verify SAN list before assuming cert reuse.
- 2026-04-19: For multi-account AWS, cross-account Terraform providers using `assume_role` with scoped IAM roles are the correct pattern. Build it once; repointing role ARN is the only change needed if accounts are reorganized later.
- 2026-04-19: Next.js static export for S3 + CloudFront requires three config values: `output: 'export'`, `trailingSlash: true`, `images.unoptimized: true`. Image optimization requires a Node runtime that static hosting does not provide.
- 2026-04-19: pnpm workspace monorepo pattern: `pnpm-workspace.yaml` at root listing `apps/website`, single root `pnpm-lock.yaml`, shared node_modules via content-addressable store. Add Turborepo only when there are cross-package build dependencies — one app does not justify it.
- 2026-04-19: Big-tech AWS pattern: management account stays empty except billing + SCPs + identity. Operational workloads never live in the management account. Violation of this is common in solo-founder environments and is acceptable tech debt to refactor post-revenue.
- 2026-04-19: Solo-founder priority ordering: (1) ship and generate revenue, (2) maintain sanity, (3) optimize architecture. In that order. Refactoring AWS layouts at 11 pm under fatigue is how DNS outages happen.
- 2026-04-20: AWS CLI on Windows Git Bash requires file://C:\path\format (not /tmp/) for --policy-document and similar parameters. Set MSYS_NO_PATHCONV=1 or use Windows paths explicitly.
- 2026-04-20: jq is not installed on Git Bash Windows by default. AWS CLI JSON output (e.g., from assume-role) parsed via shell export without jq returns empty strings. Subsequent AWS commands then run as the default identity, appearing to succeed but silently wrong. Always cross-check with get-caller-identity after assume-role.
- 2026-04-20: Windows \r carriage-returns contaminate AWS session tokens in shell pipelines. Always pipe through tr -d '\r' when extracting values from AWS JSON on Windows Git Bash before exporting as env vars.
- 2026-04-20: Terraform data sources must have globally unique names per module. data "aws_caller_identity" "current" declared twice causes validate to fail with Duplicate data configuration. Grep existing .tf files before adding new data declarations.
- 2026-04-20: Terraform cross-account provider with assume_role must specify profile (or static credentials) as source identity. Without it, alias inherits shell default credentials — which are typically the WRONG account to assume from. Pattern: profile = "psitta-prod" + assume_role.role_arn = "arn:aws:iam::TARGET:role/..."
- 2026-04-20: Scoped IAM roles that omit route53:ListTagsForResource cause data "aws_route53_zone" lookups to fail with AccessDenied. Use a local constant for the zone ID instead of the data source when minimum-privilege trumps data-source convenience.
- 2026-04-20: GitHub Actions environment: production at job level changes OIDC sub claim from repo:owner/repo:ref:refs/heads/BRANCH to repo:owner/repo:environment:production. IAM trust policies with StringLike conditions on ref: form will reject workflows with environment declared. Either remove environment OR update trust policy to allow both formats.
- 2026-04-20: pnpm/action-setup@v4 with explicit version: N input conflicts with packageManager pnpm@N.M.P in root package.json. Remove the version input or align the two values — don't both specify.
- 2026-04-20: Unauthenticated GitHub CLI exposes workflow run status/step conclusions via anonymous REST API, but not log text. Diagnosis from step timing + known failure patterns (e.g., configure-aws-credentials@v4 retry-to-60s = OIDC denial) is still possible. Run gh auth login for richer debugging.
- 2026-04-20: CloudFront custom_error_response mapping 403→404 requires the target response page (/404.html) to exist in origin. If the mapped page itself 403s, CloudFront returns the original 403 instead of looping. Empty S3 buckets naturally 403, so always upload at least index.html + 404.html before expecting 404-handling to work.
- 2026-04-20: CloudFront distribution creation via Terraform: ~3-4 minutes when reusing an existing ACM cert; 8-12 minutes when provisioning a new cert. Cert reuse is a meaningful speedup for multi-distribution architectures.
- 2026-04-20: Cross-account Terraform with assume_role takes ~30 seconds per resource during apply (STS session per operation). Total apply time is dominated by CloudFront distribution creation, not cross-account overhead.
- 2026-04-20: Windows curl via schannel TLS backend fails with HTTP/0 000 on valid certificates when CRL revocation can't complete. Use curl --ssl-no-revoke as a diagnostic. For CI, use curl-openssl variants instead of bundled schannel curl.
- 2026-04-20: AI image generators (Gemini Nano Banana, DALL-E) produce one PNG per prompt; there is no "multiple separate files in one request" mode. Build brand bundles with a STYLE LOCK paragraph + N sequential prompts pasted one at a time. Trying to request all variants in one prompt yields a composite grid, not individual files.
- 2026-04-20: AI "transparent background" output frequently bakes the checkerboard preview pattern into the PNG as actual pixels. Always verify true alpha channel before treating AI output as transparent — open in Photoshop/GIMP/PIL and inspect, or programmatically check for flat-grey corner colors.
- 2026-04-20: Tailwind v4 shipped a fundamentally different configuration model from v3: CSS-first @theme blocks inside globals.css replaced the v3 JS/TS config file. tailwind.config.ts is ignored in v4 unless explicitly opted in via @config in CSS. When generating Tailwind code for Next.js 16+ projects, always check package.json for tailwindcss version first.
- 2026-04-20: Tailwind v4 @theme block requires CSS variables to use --color-{name}, --font-{name}, --spacing-{name}, --breakpoint-{name} prefix conventions for utility classes to generate correctly. Without the prefix, classes like bg-psitta-700, text-ink-primary are present in HTML but resolve to NO CSS rules. Tailwind emits no error — utilities are silent no-ops. CONFIRM generation worked by inspecting computed styles via DevTools, not by verifying the class name appears in HTML.
- 2026-04-20: When visual CSS changes don't appear to take effect, open DevTools Elements panel and inspect the target element's Styles panel immediately. Crossed-out rules = variable/class doesn't resolve. Don't iterate on code fixes without first confirming the existing CSS is actually being applied. Three rounds of "visual fixes" were wasted tonight on tokens that weren't wired up.
- 2026-04-20: Browser extensions (Speechify, Grammarly, Honey, LastPass) inject DOM nodes at document root on every page. These appear as <div id="extension-name-*"> in DevTools. Not application bugs; but test sites periodically in incognito mode or on a clean browser to see what real visitors see without extensions.
- 2026-04-20: Case-sensitivity mismatch between Windows NTFS (case-insensitive) and Linux (case-sensitive) causes silent CI failures. A folder created as apps/website/public/Brand/ on Windows reads the same as apps/website/public/brand/ locally, but git tracks them separately AND Linux CI will 404 on /brand/ references if git has /Brand/ indexed. Always match case exactly with existing committed paths; verify via git ls-files before staging new files in previously-committed directories.
- 2026-04-20: Pre-existing CI workflows that silently fail for weeks are a common solo-founder tech debt pattern — red badges become background noise when production is deployed via other mechanisms. Queue CI remediation as its own milestone; enabling branch protection requiring green CI to merge prevents future drift.
- 2026-04-21: Pre-push code review via Claude Code saved production: email-validator missing from pyproject.toml would have caused every POST /api/v1/contact to 500. Always run a read-only verification prompt before pushing backend changes to production.
- 2026-04-21: Pydantic v2 EmailStr requires the email-validator package. Add pydantic[email] (not just pydantic) to pyproject.toml when using EmailStr. The import resolves at module load but validation fails at runtime without the package.
- 2026-04-21: ECS container does NOT auto-run alembic on startup. Dockerfile CMD goes straight to uvicorn. Migrations must be run via one-off ECS task: aws ecs run-task with --overrides command override ['alembic', 'upgrade', 'head']. Task definition :3 references :latest tag so new images are pulled automatically.
- 2026-04-21: CORS config in config.py defaults can be overridden by Secrets Manager env vars (Pydantic Settings precedence). Always verify with aws secretsmanager get-secret-value before assuming code defaults take effect in production.
- 2026-04-21: Chrome DevTools mobile device toolbar auto-requests /manifest.json for PWA heuristics. This produces a mobile-only-looking 404 in the Network tab that can be mistaken for a page 404. Always check the actual page request status, not ancillary resource requests.
- 2026-04-21: Build pages one at a time with screenshot verification between each. This catches issues early and keeps commits atomic. The M8b.2 session landed 10 commits over 8 hours with zero production regressions.
- 2026-04-21: Windows curl CRYPT_E_NO_REVOCATION_CHECK error on HTTPS: use --ssl-no-revoke flag in Git Bash. Not a server issue — Windows schannel certificate revocation check fails on some network configs.
- 2026-04-21: Footer mailto link (hello@psitta.ai) was a stale alias not matching the canonical support@psitta.ai. When replacing mailto links with route links, check for address mismatches — stale aliases in footer/header are common.
- 2026-04-22: CloudFront S3 REST origin + OAC does NOT perform directory-index resolution. default_root_object handles only the apex /. Every Next.js static-export site using OAC needs a CloudFront Function to rewrite /path/ to /path/index.html, or the entire site is broken for direct-URL access to subpages. Only the homepage will work. This bug is catastrophic but invisible during in-app navigation because Next.js SPA hydration fetches RSC payloads, not HTML files.
- 2026-04-22: CloudFront cached error responses (403 mapped to 404 via custom_error_response) persist in cache just like success responses. After fixing an origin or function bug that was causing 404s, always invalidate /* to flush the cached error pages — otherwise browsers keep getting the stale 404 until TTL expires.
- 2026-04-22: SessionStart hook's docx-parser matches on exact heading text. A V2 DevLog with heading “5. Key Learnings (append to CLAUDE.md)” was rejected with “no Key Learnings section — skipping.” Future DevLogs must use plain “Key Learnings” as the heading text to match the hook's regex.
- 2026-04-22: Gemini and most AI image generators cannot produce true alpha-channel transparent PNGs. Their “transparent background” output usually has a checkerboard pattern or cream color baked into the pixels. Workaround: use the cream-background version and apply CSS mix-blend-multiply with bg-[matching-color] so the image merges into the page background via blend mode rather than alpha.
- 2026-04-22: Next.js 16 static export requires export const dynamic = “force-static” on route-handler-backed conventions like sitemap.ts and robots.ts. Without it, the build fails with dynamic-route errors. No-op at runtime; pure build-time directive.
- 2026-04-22: Next.js Metadata API has first-class verification fields for google, yandex, me, and yahoo — but NOT bing. For Bing Webmaster Tools verification, use verification.other[“msvalidate.01”] to emit the Microsoft-specific meta tag.
- 2026-04-22: INSERT ... ON CONFLICT (email) DO NOTHING is race-free and anti-enumeration-safe for email capture endpoints. Returning the same success message for new and duplicate submissions prevents attackers from probing for list membership. Cleaner than try/catch IntegrityError — no transaction rollback needed.
- 2026-04-22: IndexNow keys are public by design. They go in plain-text files at the site root and should NOT be stored in password managers. Saving them in project notes or a plain file is appropriate; treating them like secrets is misguided.
- 2026-04-22: In 2026, Bing's index matters more than its raw search-traffic share suggests because ChatGPT and Microsoft Copilot both query Bing, not Google, when their AI agents need web search. Being in Bing's index is table-stakes for being discoverable by AI assistants.
- 2026-04-22: ECS task definition :3 references :latest tag, so new Docker images are pulled automatically on each task run without needing a new task definition revision. Migrations via one-off ECS task (aws ecs run-task --overrides command=["alembic","upgrade","head"]) remain the correct pattern and just work, as long as the new image has been pushed to ECR first.
- 2026-04-23: flutter_native_splash does not support Windows. Use an in-app Flutter widget splash with GoRouter redirect pattern (auth-aware routing to /library or /login) instead.
- 2026-04-23: Cognito Hosted UI CSS only honors documented .xxx-customizable selectors. Generic CSS rules are silently ignored. aws cognito-idp set-ui-customization requires --image-file whenever --css is provided (atomic bundle). CSSVersion timestamp is the cache-buster.
- 2026-04-23: Theme-aware assets in Flutter: use Theme.of(context).brightness == Brightness.dark with a centralized widget like PsittaLogo. Opacity can be conditional on brightness (watermark only on dark themes, full opacity on light themes).
- 2026-04-23: Pillow can generate multi-resolution Windows ICO files directly: img.save(path, format="ICO", sizes=[(16,16),(20,20),(24,24),(32,32),(40,40),(48,48),(64,64),(128,128),(256,256)]). ImageMagick is not required.
- 2026-04-23: git add + git commit without an explicit file list sweeps ALL staged files — backend, frontend, unrelated test fixtures, everything. Use explicit git add <file> <file> per commit for atomic per-commit separation (backend vs frontend vs security).
- 2026-04-23: Riverpod FutureProvider.autoDispose never disposes while ANY listener is mounted. Persistent shell widgets (sidebar, library, settings all watching billingStatusProvider) prevent refresh. Cold restart clears Dart process memory → fresh fetch with a real token → the UI recovers. This masked a latent auth bug for an unknown amount of time.
- 2026-04-23: Dual subscription table architecture is tech debt: user_subscriptions (populated by set_plan_override dev/admin PATCH) is read by the quota enforcer; subscriptions (populated by Stripe webhook) is read by /billing/status. Real Stripe customers without a matching user_subscriptions row will 402 after doc #4 despite being Pro. M9 backlog item: consolidate onto a single table.
- 2026-04-23: Quota counter increments BEFORE the document INSERT commits. Any failure after the counter bump leaves an orphan. luisaao's 50-vs-49 discrepancy was one such orphan. Fix options: (a) same transaction as the INSERT, or (b) replace the counter with SELECT COUNT(*) FROM documents WHERE created_at IN current_month AND status != 'deleted'.
- 2026-04-24: Production Flutter client sending the literal fallback string "dev-bypass-token" on null-storage race. Backend correctly rejected in production (1ms sub-millisecond 401 at JWT parse) but Riverpod cached the 401 forever → silent Free UI for Pro users. Security-critical; the fix removes the string entirely from the production binary, adds refresh+retry on 401, and introduces PlanStatus.unavailable (fail-closed).
- 2026-04-24: CloudWatch latency signature differentiates auth failure from business-logic failure. 1ms 401 = rejected at JWT parse (before handler body ran). 100-400ms 200 = handler ran (JWKS fetch + DB + audit log). When diagnosing auth issues, filter CloudWatch by latency ranges in addition to status codes.
- 2026-04-24: DOCX formatting round-trip requires THREE coordinated changes or the fix is worse than no fix: (1) backend schema accepts formatted_content, (2) client save serializes Quill Delta → block/run JSON, (3) client load deserializes block/run JSON → Quill Delta with attributes. Fixing just one leaves the other paths silently wiping formatting on save or load.
- 2026-04-24: flutter_quill 10.8.x Delta format: Document.toDelta().toList() returns List<Op>. Each Op has .data (string or object) and .attributes (Map<String, dynamic>? or Style). Inline attributes are Attribute.bold, .italic, .underline, .size (for font_size) — keyed as 'size' not 'font_size' in the Delta. Custom key-value attributes via Attribute.fromKeyValue.
- 2026-04-24: pysbd sentence segmentation and the TTS pipeline consume plain text_content only — formatting never affects audio synthesis or alignment. Safe to layer Bold/Italic/Underline/FontSize formatting on top without touching the playback code path.
- 2026-04-24: Quota dialog + banner UX pattern: proactive banner on Library (persistent, X-dismissible, reappears on next app launch if still at limit), disabled action buttons with tooltip priority (unavailable > at-limit > not-Pro > enabled), safety-net dialog for 402 responses that slip through. Replaces cryptic "DioException: bad response" with actionable messaging that shows actual plan, actual usage, and actual reset date.
- 2026-04-24: Surgical production DB writes: use ECS one-off task (same codepath as the app), composite-key WHERE clause enforced by schema natural key, UPDATE.rowcount guard that aborts if != 1, and audit_log append via audit_service.log_event for SOC 2 tamper-evident schema. Before → UPDATE → After re-SELECT → collateral count SELECT for last 30 seconds = 1. Trivially reversible via opposite UPDATE.
- 2026-04-25: ECS task definition pinned to :latest tag — force-new-deployment re-pulls without bumping revision number. Task def revision staying same after deploy is normal. Ground truth is image digest match between ECR :latest and running task's container imageDigest. Never use revision number as deploy-completed signal.
- 2026-04-25: AWS CLI cross-account — psitta-cluster lives in psitta-prod (808765744063) but default profile is Blowmymind management account. Always use --profile psitta-prod for ECS/ECR operations.
- 2026-04-25: AWS and ECS timestamps with -06:00 offset are already in local Mountain Time (MDT) — do not 'convert to local,' they're already local.
- 2026-04-25: THREE render paths exist for DOCX content, not two: Quill QuillEditor.basic (live editing), DocumentReadingView (post-save reading), and the backend /export builder (download regeneration). All three must handle every attribute symmetrically. Today found and fixed asymmetries in all three (font_size yesterday, list_type and heading-level rendering today, formatted_content reading in /export).
- 2026-04-25: Pre-launch test rot — months of CI being broken meant accumulated test failures (test_schemas.py, test_document_service.py, test_middleware/* all reference renamed/removed symbols). Unblocking CI surfaced the rot. M11 must include systematic test repair, not just lint cleanup.
- 2026-04-25: When localizing a multi-layer bug, write the regression test BEFORE picking a layer. The test of _build_branded_docx eliminated three hypotheses (export builder, python-docx interaction, level coercion) in one CI run. Beats single-shot diagnostic curls because the test stays as a permanent guard.
- 2026-04-25: Privacy-respecting observability — structural summaries (types, integer values, length counts) can be logged permanently in production. Run text and any user-typed string must never enter logs. Enforce with regression tests that include sensitive-looking strings as inputs and assert they don't appear in summary output.
- 2026-04-25: Never trust a 'bug report' from a previously-downloaded artifact. ALWAYS test against a freshly-generated download after a fix has been deployed. Today's H2/H3 mystery dissolved when we tested fresh — the artifact was stale.
- 2026-04-26: A whitelist comparator that decides run-merge boundaries MUST list EVERY schema-supported attribute; missing one produces scope-spread (when at seed position) or attribute-loss (mid-iteration). The doc comment on such a comparator must state this contract explicitly so future schema additions don't silently regress. f2acd2b is the exemplar.
- 2026-04-26: When wiring a typed value (color hex, font size, alignment) through Quill → save → storage → load → render layers, NEVER trust the apparent runtime type OR byte order. Source-trace what the upstream library emits. Flutter's Color.value is AARRGGBB; CSS hex is RRGGBBAA. Two characters of substring difference rendered red as yellow for an entire day.
- 2026-04-26: Font family attributes mean nothing without registered font assets. Flutter's font resolver silently falls back to platform default if a family is unknown — no error, no warning. The fix is either bundle .ttf assets in pubspec.yaml OR override the dropdown with platform-installed font names. For a Word-like product, platform-installed Microsoft Word fonts is the right product fit.
- 2026-04-26: When extending an observability surface that has existing brittle full-dict equality tests, AUDIT those tests in the diagnostic phase. Adding 4 new keys to a per-block summary broke 4 pre-existing tests in a separate file that asserted the old shape exactly. Ship 1 had to land in 2 commits instead of 1.
- 2026-04-26: BlockEmbed.custom in flutter_quill 10.8.5 renders correctly in the editor but has documented gaps (InsertEmbedsRule and EnsureEmbedLineRule both apply ONLY to video, not custom embeds). Save round-trip requires an explicit skip-next-newline state machine in the save path to prevent phantom paragraphs and embed-drop. A 60-minute spike validated this in time to defer page break to M13.5 instead of consuming a full day.
- 2026-04-26: When scope-spike a feature reveals 4x the original LoC estimate plus unresolved risks, defer rather than push through. Today's spike branch commit (1db3772) tagged as m13.5-pagebreak-spike is the canonical "this is what we tried" reference for resume work — tags are lighter than long-lived branches and accumulate zero rebase debt against develop.
- 2026-04-26: Hide unfinished features behind a flag (toolbar customButton omitted on develop while page_break_embed.dart, embedBuilders registration, and save/load path detection ARE on develop). This protects future data-shape collisions without exposing a broken UI surface. Re-enable in M13.5 by restoring exactly the toolbar entry and the _insertPageBreak helper.
- 2026-04-26: When an executor (Claude Code) reports an honest integrity check that questions YOUR previously-drafted commit message OR phasing, take the pushback seriously. Today: the commit message draft for the CLAUDE.md catch-up understated the diff by ~5x; Claude Code refused to push it. The correct response was to widen the commit message and split into two commits, not to override the integrity check. Senior-engineer-grade executors are worth the round trip.
- 2026-04-27: SessionStart hook's strict heading matcher (text.strip() == "Key Learnings") cannot match modern devlog heading variants like "Key Learnings from Today" or "Key Learnings (Append to CLAUDE.md, Immutable, Append-Only)". Permanent fix uses style-gated regex matcher that requires pStyle to start with "Heading" plus a flexible pattern accepting numbered/parenthetical variants. Style-gating prevents body-text false positives.
- 2026-04-27: SessionStart hook's body extractor must support BOTH <w:tbl> table format (legacy) AND <w:p> paragraph format with YYYY-MM-DD date prefix (modern devlogs). Branch on element tag. Devlogs from Apr 25 onward use the bullet format which the old extractor silently skipped.
- 2026-04-27: CloudFront URL rewrite functions only fire on URIs ending in /. Bare paths with query strings (like Stripe's /billing/success?session_id=...) bypass the rewrite and 404. Surgical fix: append trailing slash to success_url before the query string. Durable fix (extend CloudFront function) is separate deploy and goes to M9.
- 2026-04-27: Dual-table subscription architecture is real tech debt with predictable failure mode. The Stripe webhook handler writes to subscriptions table; the quota enforcer reads from user_subscriptions table. They never agree for paying users until BOTH are written in the same transaction. Pattern documented in CLAUDE.md Key Learning 2026-04-23, predicted exactly the failure mode that surfaced today.
- 2026-04-27: ECS one-off tasks (aws ecs run-task --overrides command=["python","scripts/backfill.py"]) are the correct pattern for one-off DB backfills against production. Backfill scripts must be idempotent (re-running produces zero changes), include --dry-run flag, and stay in repo as audit-trail rather than being deleted.
- 2026-04-27: Backend user auto-provisioning (check-then-insert pattern in get_current_user_id) races on the very first parallel batch of API calls for a brand-new Cognito identity. Permanent fix: ON CONFLICT DO NOTHING (no target column — catches any unique constraint violation) followed by re-SELECT. Add observability logs (user.provision_race_recovered) to track how often the race fires in production.
- 2026-04-27: Riverpod FutureProvider.autoDispose does NOT auto-dispose when persistent shell widgets (root-level ref.listen at app.dart:24) keep listeners alive across the entire app process lifetime. The cache leaks across logout/login transitions. Permanent fix: explicit invalidation via _ref.invalidate(provider) on auth boundaries (login(), logout, cold-start _init()), centralized in a helper for maintainability.
- 2026-04-27: Coverage audits before applying Riverpod invalidation fixes are non-negotiable. Initial scope of 2 providers (billing, quota) expanded to 4 (added projects, documents) plus 1 follow-up (avatar) after grepping every FutureProvider/AsyncNotifier/StreamProvider in the codebase. Without the audit, partial fixes ship and sibling bugs surface days later.
- 2026-04-27: Plan-dependent UI must gate on "billing state confirmed loaded" before rendering, never on "AsyncValue defaulting to false-on-loading". The Plans page rendered "Free as Current Plan" during the brief refetch window because isFree was deriving from a fallthrough default. Fix: explicitly track statusIsLoading and exclude both isFree and isPro during loading. Show inline "Loading your plan…" strip.
- 2026-04-27: Listeners that auto-mutate user state (like the app.dart speed/SWH downgrade-clamp on plan loss) MUST guard predicates against AsyncValue.loading transitions. The original predicate next.isPro let unavailable through and silently clamped a Pro user's saved preferences during the post-invalidation refetch window. Fix predicate to explicitly check confirmed-Free (!next.isFree). This is a data-mutation bug, not just a display bug.
- 2026-04-27: Auto-provisioning column names (auth0_user_id, @auth0.local synthetic email suffix) survive vendor migrations and become misleading naming tech debt. Today's race fix kept the auth0_* names as-is (tight scope); the cleanup is queued post-launch. Lesson: when migrating identity providers, plan a column-rename migration as part of the migration project, not as a follow-up.
- 2026-04-28: Stop hook timing — fires on Claude Code session exit, not clock time. File mtime in OneDrive vs hook firing time can mismatch by minutes when devlog is saved after /exit. Mitigation: save devlog BEFORE exiting Claude Code.
- 2026-04-28: Stripe Customer Portal config ID auto-defaults when only one configuration exists per account. Don't hardcode unless multiple configs.
- 2026-04-28: stripe.StripeError (newer SDK convention) — match the codebase's existing pattern in billing.py. Older stripe.error.StripeError is deprecated.
- 2026-04-28: SOC 2 audit consistency — every Stripe billing endpoint should write audit_service.log_event entry. Pattern: billing.checkout_session_created, billing.status_checked, billing.portal_session_created.
- 2026-04-28: Apple/iOS subscription model differs from Stripe — StoreKit 2 mandatory for digital content subscriptions, 15-30% Apple cut, separate webhook system (Apple Server Notifications), separate cancellation flow.
- 2026-04-28: Riverpod cache invalidation pattern — when launching external URL that may change subscription state, ref.invalidate(billingStatusProvider) before launchUrl ensures fresh fetch on return. Per Apr 23 Key Learning, ref.refresh can be missed when persistent shell widgets keep listeners alive.
- 2026-04-28: Bug A — sentence highlight renderer must intersect highlight with sentence boundaries when multiple sentences fall in a single Quill run. Binary overlap test paints whole run; cut-point segmentation paints only in-sentence chars.
- 2026-04-28: Bug B — proportional time-to-character mapping fundamentally breaks for long chunks (3000+ chars / 250+ seconds) with non-uniform speakable-character density. Char-count math is wrong by ~7x for early sentences in mixed-structure content. Word-count math has its own different failure mode. Lookahead offset cannot compensate for multi-second gaps. The proportional model itself is the wrong abstraction for M13-era chunks.
- 2026-04-28: Empirical-then-fix loop — read-only diagnostic with runtime debug logging proves which hypothesis is correct before any fix is written. Three Bug B attempts tonight failed because we skipped this discipline; the empirical phase came too late.
- 2026-04-28: Process — when user is solo founder with limited bandwidth, chat-Claude should not dictate architectural decisions for hard problems. Claude Code has the code in front of it. Prompts should ask Claude Code to propose options + recommend, not have chat-Claude pick the fix from outside.
- 2026-04-29: Edge TTS (rany2/edge-tts) requires explicit boundary="WordBoundary" kwarg in Communicate() constructor. Without it, ZERO WordBoundary events emit during streaming. Default behavior is silent — no error, just empty boundary stream. Always pass boundary="WordBoundary" if you need word timestamps from Edge.
- 2026-04-29: All three TTS providers (ElevenLabs, Azure, Edge) emit word-level timing data. Edge does it for free via WordBoundary events, same data shape as ElevenLabs alignment. SWH does NOT require ElevenLabs — it requires capturing whatever timestamps the active provider emits. Architectural premise of "SWH is Pro-only" was incorrect.
- 2026-04-29: Edge TTS WordBoundary offset/duration are in 100-nanosecond units (Windows FILETIME convention). Divide by 10,000 to get milliseconds. The first word's offset captures any leading silence in the audio file naturally — frontend renderer handles this correctly when alignment data is present.
- 2026-04-29: Linear-within-word character interpolation is sufficient for visual highlight purposes. ~40ms per character at typical TTS speeds is well within the precision _charIndexAtMs needs for accurate highlighting. Don't over-engineer the interpolation — exact char-level timing not needed.
- 2026-04-29: Pre-implementation schema verification (S3 sidecar dump, library API inspection) prevents schema-mismatch bugs that cost days. Always confirm wire format byte-for-byte against production data before coding to an inferred schema. Two minutes of verification saved an unknown number of debug hours.
- 2026-04-29: Unit test coverage gap pattern: tests can pass while upstream input pipeline silently produces empty data. Synthetic test inputs verify only the consumer logic. Integration probe (calling real upstream service end-to-end) verifies the producer. Always run an end-to-end probe before declaring a feature shipped.
- 2026-04-29: Test mode pattern via Secrets Manager (TTS_PROVIDER=edge then restore) is safe for pre-launch verification because there are no production users. Round-trip ~6 minutes per cycle. Useful for any provider-routing change where natural traffic doesn't exercise the new path. Always document the pre-state value (in this case TTS_PROVIDER=None) before mutating, so restoration is unambiguous.
- 2026-04-29: TTS fallback chain in tts_router.py uses two tiers: configured fallback (Tier 1 via TTS_FALLBACK env var) + Edge hardcoded (Tier 2 last resort). Pre-Apr-29 Edge had no alignment so this ordering was correct. Post-Apr-29 Edge has alignment, Azure does not — chain inverted via code swap (commit ee1bc76). Now: ElevenLabs → Edge → Azure. TTS_FALLBACK="azure" (default) preserved as Tier 2 backstop.
- 2026-04-29: v1 pricing locked: Free $0/mo / Reading Nook Pro $99/yr or $14.99/mo (150k ElevenLabs chars/mo) / Creativity Nook Pro $299/yr or $34.99/mo (400k chars + 2M LLM tokens + voice cloning, HIDDEN at v1 launch shown as "Coming soon" with waitlist). Annual is primary offer, monthly is premium. Margin ~51% at 100 users.
- 2026-04-29: AWS sizing verified: ECS Fargate 0.5 vCPU / 1 GB single task (~$5.50/mo at baseline), RDS db.t4g.micro single-AZ (~$14.30/mo), ALB always-on (~$22/mo, largest single line item), total AWS ~$60/mo at 100 users. Largest fixed-cost line is ALB. AWS amortizes well across users — gets cheaper per-user at scale.
- 2026-04-29: PDF word-highlight uses pdfrx character bounding boxes (positional rendering), not TextSpan backgrounds like DOCX. Word highlight on PDF is currently positionally offset (spans space between two words). Separate code path from DOCX highlighting. Filed as post-launch polish — not blocking v1 ship.
- 2026-04-29: Process discipline that worked: read-only diagnostic → option analysis with Claude Code recommending → user approves → implementation in halves with checkpoint between → end-to-end probe before commit → deploy → verify in production → restore (if test mode) only after verification. Three commits today, zero reverts. Compare to April 28 night: three Bug B attempts, all reverted.
- 2026-04-30: Stripe sandbox-pre-verification accounts silently drop subscription_update.products field via API GET even when Dashboard saves the allowlist — runtime enforcement still works. Verify on launch day post-EIN/bank that this serialization issue resolves.
- 2026-04-30: Stripe transfer_lookup_key=True atomically reassigns canonical lookup_keys from old → new prices on the same product. Old price's lookup_key drops to None.
- 2026-04-30: Stripe default Customer Portal config can't hold a products allowlist via API mutation — silently rejected. Dashboard config works at runtime regardless.
- 2026-04-30: Stripe product default_price must be swapped before archiving the old price (can't archive a price set as default).
- 2026-04-30: CloudFront Functions can do both URI rewrites and 301 redirects — combining them lets a single function handle trailing-slash AND host redirects. Query string preservation requires a buildQs helper since CloudFront Functions parse querystring into object form.
- 2026-04-30: CloudFront default_root_object only handles '/' — directory indexes for subpaths require explicit URI rewrite to /path/index.html.
- 2026-04-30: S3 with OAC (Origin Access Control) doesn't natively resolve directory indexes — different from S3 website endpoint which does. CloudFront Function must rewrite.
- 2026-04-30: Two-commit migration pattern (schema first, code second) is genuinely safer than single-commit. ~3 minute extra cost, eliminates timing dependencies. Use for any migration touching production.
- 2026-04-30: signup_list.first_name was previously NOT NULL because original signup_list endpoint required it. Dropping NOT NULL via Alembic migration was the correct fix — F1a empty-string sentinel would have been data-quality debt.
- 2026-04-30: apps/website existing SignupForm and ContactForm follow a consistent pattern — when adding a third (CreativityWaitlistForm), reuse the pattern but don't refactor into a generic component during launch week. Refactor post-launch when the right abstraction is clear.
- 2026-04-30: Phase D + F web pages pattern: build phase-by-phase, deploy each phase separately, verify with curl + browser before next phase. Allows rollback of individual phases if needed.
- 2026-04-30: Process discipline today: 10 commits, zero reverts. Diagnostic-first → options → recommendation → user approval → checkpointed implementation → end-to-end probe → deploy → verify. This is genuinely big-tech approach for a solo founder.
- 2026-04-30: Free Trial architecture decision (v1.1 work, not v1): 15-day Free Read (no card) + 14-day R-Pro trial (card required at trial start, not signup). Total free runway 29 days for engaged users. Email cadence locked at 2 emails per Free window (welcome + locked) plus Stripe-automatic transactional emails — zero marketing nags.
- 2026-04-30: Re-signup detection: lowercase email normalization at signup, no plus-stripping (defer). Expired account triggers 'Your free trial expired on [date]. [Upgrade] or [Sign in]'. Never-used returning users get no fresh window. GDPR-deleted users get fresh window (legal requirement).
- 2026-04-30: Grandfathering early adopters: tier by usage, not by signup date. 3+ docs + audio = lifetime R-Pro. 1-2 docs minimal = 90-day R-Pro free. 0 docs = standard 15-day clock at v1.1 launch. SQL classification query stored in DevLog for future ECS one-off task execution.
- 2026-04-30: Free Trial implementation phasing: T1 (free window enforcement, 1.5d) + T2 (R-Pro trial flow, 2d) + T3 (email funnel + legal, 1.5d) = ~5 days work. Explicitly does NOT ship before v1 launch — v1 ships with current Free Read indefinite, trial pivot is v1.1 work.
- 2026-05-01: SessionStart memory-hook can fail silently — Stop-hook mechanical work (Last Devlog pointer, recent commits) succeeded but the model-synthesis half (parse devlog, append Key Learnings) did not fire. Manual recovery via boundary detection + verbatim append from previous DevLog is a reliable fallback. ~2 minutes to execute. Always check 'grep -c' counts after morning sync to detect this case.
- 2026-05-01: There are TWO PLAN_LIMITS dicts in the Psitta backend (intentional dual-state during Stripe migration): legacy in subscription_service.py keyed by 'free' / 'pro_monthly' / 'pro_annual' (matches user_subscriptions.plan_id Postgres ENUM), and new in plan_limits.py keyed by 'free' / 'reading_nook_pro' / 'creative_nook_pro'. Resolve any future drift between them with normalization at the boundary, NOT by adding aliases to either dict.
- 2026-05-01: user_subscriptions.plan_id is a Postgres ENUM, not a string column. Renaming canonical values requires a schema migration to widen the ENUM. Don't propose 'rewrite at write time' fixes without first checking the column type.
- 2026-05-01: Stripe webhook handler (billing_handlers.py) collapses Reading Nook Pro AND Creative Nook Pro lookup_keys onto the same pro_monthly/pro_annual codes via _LOOKUP_KEY_TO_PLAN_ID. Both products share identical quotas in the legacy dict. C-Pro vs R-Pro differentiation lives elsewhere (lookup_key in subscriptions table, parsed in billing.py:_parse_lookup_key).
- 2026-05-01: Riverpod billingStatusProvider is FutureProvider.autoDispose<Map<String, dynamic>>. Apr 27 fixes (f88bf14 + d75c751) added invalidation at auth boundaries via _invalidateUserScopedFetches. The remaining gap was app-resume after external-browser side-trip — no listener existed for window-focus or AppLifecycleState.resumed before today's commit e1a1619.
- 2026-05-01: Flutter desktop lifecycle semantics differ from mobile: a 5-minute spike with debugPrint() in didChangeAppLifecycleState verified that AppLifecycleState.inactive fires reliably on Windows alt-tab. By symmetry resumed fires on return. window_manager.WindowListener.onWindowBlur/onWindowFocus also work — held as fallback.
- 2026-05-01: Spike-and-revert pattern: instrument code temporarily (debugPrint), run flutter run -d windows, capture empirical platform behavior, then immediately git checkout to revert before shipping the real fix. Cheap insurance against committing to the wrong primitive.
- 2026-05-01: Item B (commit e1a1619) implementation: PsittaApp converted ConsumerWidget → ConsumerStatefulWidget to host WidgetsBindingObserver. On AppLifecycleState.resumed, invalidates billingStatusProvider AND quotaUsageProvider. 5-second throttle prevents hammering on rapid alt-tab. First resume always fires (throttle DateTime initialized to epoch). Existing 30-second poll on PlanSelectionScreen retained as redundant safety net.
- 2026-05-01: Verification pattern for Item B: force-sync DB to a known state (Path C — both subscriptions + user_subscriptions tables in single transaction with RETURNING), then alt-tab to verify desktop picks up the change. Tests Item B in isolation from Stripe webhook handler. Both directions (Pro→Free and Free→Pro real Stripe checkout) verified PASS.
- 2026-05-01: Free Trial pivot LOCKED May 1: defer to v1.1, NOT pre-launch. Trigger to start T1+T2+T3 work is 30+ days post-v1-launch AND C-Pro feature-complete. Decision gate is data on 100+ real users for ≥30 days. Window sizes (15-day Free Read + 14-day R-Pro Trial) match Notion/Linear/Figma standard — don't drift pre-emptively without funnel data.
- 2026-05-01: Industry pattern check: Notion shipped trial mechanics in year 4, Linear in year 3, Figma year 5+. None shipped paid auto-renewal trials at v1 launch. Comparing v1 launch day to mature subscription apps is a category error. Ship core product, instrument funnel, design trial against real conversion curves.
- 2026-05-01: Website /pricing surface needed match-to-desktop sync (commit 97790da). Pattern: extract bullet arrays from desktop source-of-truth file FIRST, then port to website with active/excluded/coming-soon row variants. Don't sync product/page.tsx to /pricing — different surfaces with different purposes (deep-dive vs comparison shopping). Removed misinformation 'free trial' line from page bottom — would have been load-bearing wrong claim at v1.
- 2026-05-01: Pre-existing CI test rot (test_schemas.py, test_middleware/* — collected in M11) means Test Backend job has been red since Apr 25. New test files (like Item P's test_plan_limits.py) inherit the failure because conftest collection dies upstream. Not a regression from new commits — must verify the failure signature matches parent commits to confirm it's the existing rot, not a new break.
- 2026-05-02: Stripe Basil API (March 31, 2025) moved current_period_start/end from subscription root to subscription_item objects. Stripe Python SDK auto-upgraded past v15.x silently breaks code reading sub.get('current_period_end') at root. Always read period fields from items.data[0].current_period_start in handle_checkout_session_completed and handle_subscription_updated. Pin SDK with explicit major-version bound (>=15.0.0,<16.0.0) — major-version float is silent breakage waiting to happen.
- 2026-05-02: Defense-in-depth for billing duplicates: (1) UI hides Subscribe button when user is Pro, (2) backend Stripe-direct check (stripe.Subscription.list status='active' before creating session), (3) DB partial unique index on subscriptions(stripe_customer_id) WHERE status='active'. Three layers each backstop the others. Just ship all three — they're cheap.
- 2026-05-02: Local DB pre-checkout duplicate guard is webhook-state-dependent (vulnerable to webhook lag — first sub's webhook hasn't processed when second checkout hits). Stripe-direct check via Subscription.list is the authoritative source. Local check stays as fast-path early-out, Stripe check is the real defense.
- 2026-05-02: ON CONFLICT (stripe_subscription_id) DO NOTHING in webhook handler protects against same Stripe sub being inserted twice (Stripe retries) but does NOT detect 'this user already has a different active sub.' Distinct sub_xxx IDs both get rows. The partial unique index on (stripe_customer_id) WHERE status='active' is what blocks duplicates from the same customer.
- 2026-05-02: /billing/status query intent is 'find this user's active subscription, if any.' Filter to status='active' in WHERE clause, not via ORDER BY tricks or post-filter. Picking most-recent row regardless of status breaks for any user who cancels and resubscribes — canceled row becomes most recent, endpoint returns Free despite Stripe billing them.
- 2026-05-02: Audit logging the success path only is an observability gap. /billing/status was logging only on Pro returns, not on Free returns. Free returns invisible to forensics — wasted ~30 min today not knowing what the endpoint was actually returning. Move audit_service.log_event before early returns, log every code path with a forensic outcome field (no_customer / no_active_sub / found_active).
- 2026-05-02: Backfill script three-stage execution pattern: --dry-run --limit 1 (smoke test, verify Stripe path works on one row), --dry-run (preview all rows, sanity-check would-write values), live run (no flags, commits row-by-row). Stage between human reviews. Bug B's email backfill was correctly aborted at Stage 1 because dry-run revealed synthetic @auth0.local source data — running live would have made things worse.
- 2026-05-02: Idempotent-on-DB backfills naturally exclude already-processed rows via WHERE NULL filter. Re-runs after success process zero rows. This is genuinely safer than 'we already ran it' tracking — the data itself is the cursor.
- 2026-05-02: Dockerfile must explicitly COPY scripts/ to runtime image for the aws ecs run-task --overrides command pattern to work. Documented one-off script pattern was apparently never exercised post-image-build until today's backfill. Two-line fix unblocks future scripts. Add 'image-includes-scripts' to deploy validation checklist.
- 2026-05-02: Synthetic email tech debt from auto-provisioning blocks naive Stripe email backfill. users.email contains UUID@auth0.local placeholders for users that landed via the get_current_user_id ON CONFLICT auto-provisioning path. Backfilling Stripe emails from this column would replace stale-but-real test1@facti.ai with synthetic placeholders. Strictly worse outcome. Fix users.email from Cognito User Pool first (cognito-idp.AdminGetUser), then backfill Stripe.
- 2026-05-02: 'Is the money flow correct' is not the same as 'is the billing relationship correct.' Bug B initially classified cosmetic because payments routed correctly via psitta_user_id metadata. User correctly pushed back: receipts to wrong inbox = GDPR + chargeback risk + account-recovery confusion. Wrong email = wrong relationship even if money lands right. Re-classify severity when user surfaces a real risk angle that wasn't on radar.
- 2026-05-02: Verification debt is a real liability. Stacked 5 unverified fixes mid-day before user called it out. New rule: no new commit until previous commit is verified end-to-end. Stop-and-test discipline is non-negotiable. The fix-stack-up pattern is how production bugs hide between commits.
- 2026-05-02: flutter_secure_storage_windows DpapiJsonFileMapStorage tries to delete the .dat file during read (atomic rewrite pattern). Repeated kill-restart cycles of flutter run during dev testing can leave the file in a state where the same Psitta process can't delete a file it has open. Symptom: PathAccessException errno 32 + auth failures + UI defaults to logged-out / Free state. Fix: rm the .dat file, sign in fresh. Dev-environment artifact only — real users install MSIX once.
- 2026-05-02: EL quota counter design: keyed by (user_id, period_start) where period_start = subscriptions.current_period_start (billing anniversary), NOT by calendar month. Annual subscribers starting May 1 reset on May 1 every year, not month rollover. Distinct model from existing usage_counters (calendar-month docs counter) — different reset cadences, different keys, both correct for their domain.
- 2026-05-02: ElevenLabs primary path verified end-to-end after May credit reset: Rachel voice, alignment=yes (word timestamps power S.W.H), tts_router.ok provider=elevenlabs in CloudWatch. Fallback chain configured EL → Edge → Azure all ready states. Forensic confirmation via CloudWatch tts_router events with provider field — definitive evidence which provider actually served the request.
- 2026-05-02: Item C phasing: C.1 (DB + plan_limits dicts), C.2 (service layer + router instrumentation + graceful degrade), C.3 (webhook period reset + endpoint extension), C.4 (desktop UI). Phased commits keep each one verifiable independently and let stop-points catch design issues without rolling back hours of work. Don't ship Item C as one giant commit.
- 2026-05-03: Item C phasing pattern is reusable for any feature with DB → service → endpoint → desktop layers. Split into N small commits with stop-points between each. When bugs surface (Bug-Auth-1, audio regression), they're contained to a single phase's diff and trivial to diagnose. If C.1-C.4 had shipped as one 800-line diff, today's bugs would have been buried.
- 2026-05-03: Adding Depends(get_current_user_id) to a previously-unauthenticated endpoint can expose pre-existing latent bugs — bare Dio in audio_service.dart was missing the Cognito auth interceptor for months. Pre-C.2 the endpoint accepted unauthenticated requests so the bug never manifested. C.2's correct security posture surfaced it. Always check both ends (caller and callee) when tightening auth on an endpoint.
- 2026-05-03: Cross-user resource scoping requires WHERE filter on owner_id, not just JOIN to owner table. JOIN documents d ON d.id = c.document_id without AND d.user_id = :uid is a security bug. The JOIN provides relationship resolution; the WHERE provides authorization. Both required. Audit all multi-tenant SELECT queries for this pattern.
- 2026-05-03: Return 404 (not 403) on owner mismatch for resource access. Returning 403 leaks the existence of the resource to unauthorized users ("this exists but you can't see it"). 404 is indistinguishable from "resource does not exist" — no information disclosure. Industry-standard pattern (GitHub, Stripe, etc.).
- 2026-05-03: Diagnostic-first when production behavior surprises you, not when you assume the cause. Initial wrong assumption today: audio playback regression must be Bug-Auth-1's d.status != 'deleted' filter. Diagnostic showed real cause was C.2's auth requirement exposing the bare-Dio bug. If we'd reverted Bug-Auth-1 reflexively we'd have reintroduced 6 security bugs to fix a non-issue.
- 2026-05-03: Cache key comparison logic must use the same source on both sides of the equals. Lines 1268-1273 in documents.py compared chunk_text (post-cleaning, varies by source_type) to chunk_row.text_content (raw from DB). For PDFs where cleaning modifies the text, chunk_text != chunk_row.text_content always. Cache check failed on every play. The comparison was both paranoid AND broken.
- 2026-05-03: When the same chunk can be served from multiple endpoints (audio + alignment), they must share cache state. Pre-Bug-EL-1, /audio and /alignment each independently called EL on first play, billing 2x. Fix: /audio uses synthesize_with_alignment_and_quota and persists alignment sidecar so subsequent /alignment hits cache. Single bill per chunk regardless of endpoint order.
- 2026-05-03: User precision feedback catches launch-blocker bugs system tests miss. User noted 23-char document → 363 chars charged. Without that pushback we'd have shipped a 2x billing inflation that power users would have disputed publicly. When user makes quality argument from first principles, investigate even when system appears 'working.'
- 2026-05-03: Period-keyed quota model (billing-anniversary aligned via subscriptions.current_period_start) requires UNION fallback to user_subscriptions for dev-override Pro users. _get_active_period_start in subscription_service.py implements this. Stripe-paying users hit subscriptions table; dev-override Pro users (set_plan_override) hit user_subscriptions. Both paths supported; consolidation deferred to v1.1.
- 2026-05-03: Cache-hit logging is cheap forensics for billing disputes. Bug-EL-1 took 10 minutes to diagnose because we lacked audio.cache_hit events; only audio.cache_miss was logged. Now both events fire with kind discriminator. Future billing-precision investigations become trivial. Cost: ~5 chunks/play × N plays/day in CloudWatch ingest. Acceptable, easily suppressed if it ever shows in billing.
- 2026-05-03: Toast suppression via period-stamped SharedPreferences key is genuinely idempotent. Key: user_${userId}_el_quota_toast_${periodIso}. When elCharsResetAt rolls forward (next renewal), the key changes, the toast becomes fresh again. No manual cleanup needed (old period keys are tiny — bool per past period per user, ~12/year). Pattern works for any 'show once per period' UX.
- 2026-05-03: ref.listen at top of Riverpod build() is correctly canonicalized — multiple builds don't stack listeners. Library screen ref.listen(quotaUsageProvider, ...) fires only on AsyncValue transitions, not on every rebuild. No perf concern. Good pattern for cross-screen reactive notifications (toasts, banners) where the data lives in a provider.
- 2026-05-03: Stable executable filename (psitta.exe, not desktop.exe or psitta_v1_0_0.exe) matches industry convention — Slack, Discord, Chrome, Notion, VS Code. Version metadata lives in MSIX manifest + Settings → About + CloudWatch logs, NOT in filename. Stable filename supports auto-updaters, doesn't break shortcuts/taskbar pins on update. Bundle rename with MSIX rebuild for clean single-commit change.
- 2026-05-03: Industry-standard rollout pattern: Internal Alpha (5-15 personal contacts, cloud drive, Stripe sandbox) → Closed Beta (50-150 curated, token-gated, live Stripe with discount) → Public Beta (self-serve, full pricing) → Public Launch (Microsoft Store + ProductHunt + PR). Each stage has concrete exit criteria; don't advance on calendar alone. Notion, Linear, Arc, Superhuman, Loom all followed this pattern. Documented as Tester Distribution Plan v1.0.
- 2026-05-03: Margin of error in EL char counting (~3-5 chars per chunk = ~1% on 1,000-char real-world docs) is acceptable. Sources: trailing newline from DOCX upload, TTS smoothing space, smart-quote conversion, possible BOM. Variance scales sub-linearly with document size. Users tracking quota will see 1,000-char doc charge ~1,003-1,005 — they round to 1,000 anyway.
- 2026-05-03: Don't reset test account quota counters mid-launch-prep for 'cleanliness.' Test3's counter was inflated by Bug-EL-1's pre-fix double-billing. Tempting to reset for clean baseline, but: (a) test accounts are testing infrastructure, not real customers, (b) precision tests use deltas not absolute values, (c) inflated counter teaches us 'this is what doubled looks like' for future debugging. Audit-trail purity > convenience.
- 2026-05-04: Hostinger Email + Route 53 separation: DNS hosting (Route 53 zone Z0157844SO3Z3QTLZ9UB in mgmt account) and email hosting (Hostinger Business Email plan) are independent. Adding psitta.ai email keeps DNS in Route 53 — only adds MX/SPF/DKIM/DMARC records.
- 2026-05-04: TXT record UPSERT pattern for multi-value coexistence: read existing values via aws route53 list-resource-record-sets, append new value as another quoted string, UPSERT all values atomically. Used twice today — Hostinger TXT verification + SPF.
- 2026-05-04: msix Dart package auto-trims transparent borders before resizing — pre-padded source PNG margin gets stripped during MSIX packing. The .exe icon (app_icon.ico, our direct control) preserves intended margin; MSIX tile auto-generated assets do not.
- 2026-05-04: Browser .appinstaller behavior: Chrome AND Edge both download the file rather than auto-launching Windows App Installer. User must open the downloaded file. Document in install instructions as 2-step flow.
- 2026-05-04: Cloudflare Email Routing only works for domains hosted in Cloudflare. psitta.ai DNS in Route 53 → must use Hostinger Email Hosting OR AWS SES OR third-party forwarder (ImprovMX).
- 2026-05-04: 'Founder-led support' phrasing reads as scrappy/early-stage; 'support team' reads as mature/scalable. Public-facing copy should signal stable team even when solo behind the scenes.
- 2026-05-04: Big tech analytics pattern at solo-founder stage: Plausible ($9/mo) + Stripe dashboard + Hostinger inbox + spreadsheet covers 90% of 'what's happening' need. Custom admin panel is premature optimization until 10+ users to dashboard.
- 2026-05-04: Cross-account Terraform pattern stable: psitta-website-route53-manager role in mgmt account (010526248733), aws.blowmymind provider alias, used for download.psitta.ai records. Reusable for any future psitta.ai subdomain.
- 2026-05-04: Email alias vs forwarder distinction: alias keeps inbound + allows reply-as within domain (preserves SPF/DKIM/DMARC); forwarder sends mail outside domain (reply identity mismatched). Always use alias when staying in-domain.
- 2026-05-04: /support copy convention: 10-FAQ via native <details>/<summary> is zero-JS, accessible, SEO-friendly (search engines see all Q+A in DOM regardless of open state). Pattern reusable for future FAQ surfaces.
- 2026-05-04: AppInstaller XML schema: <Hash> element does NOT exist in any version. MSIX cert signature is the integrity story. Schema is http://schemas.microsoft.com/appx/appinstaller/2018.
- 2026-05-04: Distribution surface global via PriceClass_100: US/Canada/Europe edges, but routes from anywhere in the world (just slower from APAC/LatAm). Upgrade to PriceClass_All when traffic justifies — current scale doesn't need it.
- 2026-05-04: Internal Alpha install flow proven end-to-end May 4: psitta.ai/download → click Install button → psitta.appinstaller downloads from download.psitta.ai → open file → Windows App Installer dialog → Install → version 1.0.5.0 active → auto-updates every 24h.
- 2026-05-04: Founder name visibility at 0-1k user stage: keep visible. Linear, Notion, Loom, Substack all kept founders front-and-center early. 'Founder & CEO' signals incorporated commitment; pure 'Founder' alone reads as moonlighting hobby project.
- 2026-05-04: Network architecture decision: 7 of 8 nav surfaces already aimed at /download — diagnostic-first surfaced this and prevented speculative refactor. Single-button edit instead of architectural rework. Diagnostic phase pays off.
- 2026-05-04: Auto-suggested input in Claude Code terminal is NOT user confirmation. Always wait for user-typed confirmation before proceeding to commit/push. Twice today the suggestion-template was misread as confirmation.
- 2026-05-04: Hostinger Webmail session cookie collision: clicking 'Webmail' on a new mailbox card can open the existing session's mailbox. Resolution — sign out and re-authenticate as the new mailbox, or use a private/incognito browser window.
- 2026-05-04: Big tech 'support team' phrasing is correct for public surfaces even with a solo founder. The Footer attribution can preserve company naming ('Built and supported by Facti AI LLC') without the public-facing copy advertising solo operation.
- 2026-05-05: Tester pathway architecture decision LOCKED: Pattern 3 (backend allowlist with 30-day TTL) chosen over Pattern 2 (Stripe coupon). Same MSIX as public users. No Stripe production touch until public launch. Coupons require Stripe live-mode setup + force 12-step checkout including card entry; allowlist gives 5-step UX with zero card friction.
- 2026-05-05: Cognito JWT access tokens currently DO NOT include email claim. TokenClaims.email defaults to '' (empty string) for all production users. Auto-provisioning substitutes '<cognito-sub>@auth0.local' for users.email. Affects all email-keyed features. Fix planned as v1.1 backlog: emit email claim + AdminGetUser backfill.
- 2026-05-05: Resolver bug found during T11.3b smoke prep: 'if email is None' returns False for empty string, silently skipping allowlist fallback. Fix: 'if not email' treats '' and None identically. Real correctness bug affecting all production allowlist users until fixed (commit 18565af).
- 2026-05-05: Apr 17 KL re-applied during T11.3a: audit_log.resource_id is UUID-typed. For non-UUID identifiers (emails, etc.), pass resource_id=NULL and put the identifier in details_json. Caught in implementation phase before commit.
- 2026-05-05: Resolver pattern (T11.2) collapses Apr 23 dual-table tech debt as side effect: single get_effective_plan(db, user_id, email) is now the entitlement source of truth. Existing helpers (_get_active_plan_id, _get_active_period_start, check_el_quota) become thin wrappers. Stripe-paying users with no user_subscriptions row now correctly enforce R-Pro quotas (was wrongly returning 'free' pre-T11.2).
- 2026-05-05: ECS one-off task pattern for admin scripts: aws ecs run-task --cluster psitta-cluster --task-definition psitta-api --overrides command=['python','scripts/<script>.py',...]. Backend scripts/ directory auto-COPYd into Docker image (Dockerfile line 68). New scripts placed in core/backend/scripts/ ship without Dockerfile changes.
- 2026-05-05: Voice avatar pattern for theme-adaptive design: ring/glow renders at runtime in Flutter using ThemeData accent color (Paper Light blue / Rose Salmon pink / Beige Gold Navy gold / dark variant) — NOT baked into PNG asset. PNG is face-only with transparent background. Future themes work automatically.
- 2026-05-05: Per-voice override dict pattern in cropping script (PER_VOICE_BIG, PER_VOICE_SMALL): handle outliers without complicating default path. Pattern: default coordinates apply to all; override dict adds per-image specific coordinates. Used for Arnold's big-circle 7px offset.
- 2026-05-05: Per-row ring-boundary scan technique for circle detection: at each y in target band, find leftmost/rightmost ring-color pixel; row with maximum inter-distance is true vertical center. Trumps horizontal-scan diameter measurement which can catch a chord rather than the diameter (caused Adam big-circle eyes-level y=354 → corrected to true center y=445).
- 2026-05-05: Smoke test catches silent failures unit tests miss. T11.2 unit tests passed by passing real email strings; production traffic uses empty strings; different paths, same code. Visual smoke caught Fix 1 bug before any tester would have seen it. Diagnostic-first protocol's smoke step is non-negotiable.
- 2026-05-05: Big tech analytics pattern at solo-founder stage held firm: Plausible ($9/mo) + Stripe dashboard + Hostinger inbox + spreadsheet covers 90% of 'what's happening' need. Custom admin panel deferred until 10+ testers. Decision held against Luis's instinct to build dashboard early.
- 2026-05-05: Allowlist + interest-confirmation gate is the cleanest tester onboarding pattern: outreach copy invites a reply ('let me know if interested') rather than sending allowlist access in cold message. Filters serious testers from casual browsers. Magic UX once confirmed: tester reply → allowlist add (1 DB insert) → reply 'install at psitta.ai/download' → tester sees R-Pro instantly.
- 2026-05-05: Soft-revoke pattern for tester_allowlist (revoked_at TIMESTAMPTZ, NULL = active): preserves audit trail. Active queries filter WHERE revoked_at IS NULL AND expires_at > NOW(). Revoked rows physically remain — SOC 2-correct behavior, harmless, demonstrates revoke→audit chain worked.
- 2026-05-05: Resolver email fallback: when claims.email is empty/None, fall back to users.email lookup. When users.email is synthetic (Apr 27 KL), allowlist won't match unless allowlist row keyed on synthetic email too. Two-bug compounding: Fix 1 fixes the empty-string fallback; full fix requires Cognito email claim emission + backfill (v1.1).
- 2026-05-05: Stripe production setup is gated to public launch day, not earlier. Testing live mode early via $0 coupon transactions pollutes real Stripe data. Pattern 3 keeps Stripe untouched until you're truly ready for paying customers.
- 2026-05-06: SessionStart hook flake pattern: pending_session_summary.json present at session start = hook didn't fire/consume. Manual sync protocol works: backup → read newest DevLog by mtime → extract KLs → append between last dated entry and "## Last Devlog" header → update pointer → delete pending file.
- 2026-05-06: CI is informational, not gating, on direct push to develop. Release workflow runs independently with its own Docker build. "Test failures block deploys" was a wrong interpretation — test failures only block PR merges. Direct pushes to develop deploy regardless of CI status.
- 2026-05-06: Audio cache persistence: synthesize endpoint caches audio per (text, voice_id) at the server side. Pre-fix audio survives in cache and blocks visual verification of TTS routing changes. Fresh content (new text) bypasses cache. Always test code-path changes with new content, not replays.
- 2026-05-06: Theme-page-background interaction with source-image content: Rose theme's salmon background interacts visually with ElevenLabs' lavender source wash, producing perceived dullness. Not a source-quality issue. Other themes (Parchment, Amber, Midnight) unaffected. Polish item, deferred to v1.1.
- 2026-05-06: Diagnostic-first calibration: aggressive bundling for low-risk local-script work (PIL cropping iterations); strict checkpoint protocol for backend/deploy work. "For low-risk uncommitted local script work, batch more aggressively. For backend, deploys, infra, prod data — keep the strict protocol."
- 2026-05-06: Cognito email-claim issue manifests in Voices grid as Free-tier UX. Resolver fallback to synthetic users.email doesn't match allowlist row keyed on real email → returns Free → Voices grid locks 12 ElevenLabs (premium) voices, leaves 6 Azure (standard) unlocked. Broader visible surface than Item 11.4 originally scoped (not just Settings).
- 2026-05-06: Catalog-driven dispatch is the architectural pattern for multi-provider routing. Provider field on catalog must drive dispatch — not naming conventions, not regex pattern-matching, not parallel mapping dicts. Naming/pattern routing is brittle; catalog-driven eliminates maintenance debt with each new voice.
- 2026-05-06: Per-template variance in source images requires per-voice override approach. Single batch override fails when generators differ. Variance >3 px on any axis = use individual overrides; <3 px = single batch acceptable.
- 2026-05-06: Edge TTS native voice ID format: ^[a-z]{2}-[A-Z]{2}-\w+Neural$. Pattern-passthrough in defensive layers handles native Microsoft IDs without translation table maintenance. Use as defense-in-depth, not primary routing.
- 2026-05-06: Voice avatar source dimensions: ElevenLabs 937×1136 RGBA, lavender ring at center (630, 445) radius 211. Azure 937×1136 RGBA (same canvas), but cx≈636 with dy varying per voice 436-450. Per-voice PER_VOICE_BIG overrides needed for Azure variants.
- 2026-05-06: Edge-walk algorithm beats span-finding when content is non-circular inside a circular ring. Span-finding (max non-lavender bounded gap per row) lands on a chord at wrong y when face content extends past the geometric inner edge. Edge-walk (walk outward from interior point until ring detected) gives true vertical diameter. Horizontal walks only give true diameter at the true cy.
- 2026-05-06: ECS deploy verification: ECR :latest digest must match running ECS task containerImageDigest. Wall clock from push to digest match: ~10-12 min for backend changes, ~6-7 min for desktop-only changes (smaller image rebuild). ECR pushedAt is the deploy-started signal; ECS converging on the new digest is the deploy-complete signal.
- 2026-05-06: scripts/ directory is COPYed into the runtime Docker image. Desktop-only changes that touch scripts/ produce different ECR digests despite zero functional change. Image differs but FastAPI doesn't invoke the new file. Worth knowing for image bloat tracking.
- 2026-05-06: VoiceAvatar widget pattern: single class for both small (128px) and big (110+) variants; auto-detect by size > 64; asset path resolved from voiceName.toLowerCase() + size prefix; Image.asset errorBuilder fallback to person_outline icon; theme-adaptive ring via PsittaTokens.glow with caller-provided override for selected state.
- 2026-05-06: Unit test isolation pattern for FastAPI services: ClassName.__new__(ClassName) bypasses __init__ (which would attempt real provider connections); inject mocks for collaborators via test factory; autouse fixture clears lru_cache between tests to prevent stale-lookup pollution. Mock-based tests verify dispatch logic without HTTP, run in <1s.
- 2026-05-06: Stop hook fires on session end and creates .claude/pending_session_summary.json (the marker for SessionStart hook to consume next session). The marker mtime tells you when Stop hook ran. If it exists at SessionStart, SessionStart skipped/failed — diagnostic signal for hook flake.
- 2026-05-06: Big-tech-reliable approach to TTS dispatch fix: Option 3 (catalog-driven) over Option 1 (identity-map bandaid) or Option 2 (regex passthrough). Reasoning: the catalog field already exists for this purpose; using it eliminates maintenance debt every time a voice is added; one-time architectural fix beats accumulated dict entries.
- 2026-05-07: Reddit is blocked at the Anthropic crawler domain level for both web_search and web_fetch. Discovered during psitta-outreach-research skill first invocation. Cannot research Reddit candidates via Anthropic web tools. Workarounds: (a) PRAW (Python Reddit API Wrapper) with Reddit OAuth — proper API path, ~30-60 min build; (b) manual browser research, paste candidate JSON into skill via future input parameter; (c) skip Reddit. Decision deferred until LinkedIn + Substack candidate quality assessed.
- 2026-05-07: psitta-outreach-research skill v1.0 does not filter by location. First run produced mix of US + international candidates. For v1.1: add optional location_filter parameter (default: "US"; accepts list like ["US", "UK", "Canada"]) that constructs platform-specific location operators in WebSearch queries. LinkedIn supports "geo:" filter; Substack location is in author bio (less reliable filter, would need post-fetch filtering). Defer to v1.1; for now Luis filters manually during xlsx review.
- 2026-05-07: psitta-outreach-research skill v1.1 enhancement spec — Location field population.

  Required format:
    US-State-CityAbbrev    -> "US-Boulder-CO" / "US-Brooklyn-NY" / "US-San Francisco-CA"
    Non-US                 -> Country-City-Subdivision  -> "Canada-Toronto-ON" / "UK-London-ENG" / "Brazil-São Paulo-SP"
    Subdivision absent     -> Country-City              -> "UK-Manchester" / "Australia-Sydney"
    Missing                -> "Unknown"

  Implementation approach for v1.1:
    1. LinkedIn extraction: WebFetch full profile page; parse location string from .pv-text-details__left-panel or .text-body-small.inline.t-black--light section. Handle "City, State, Country" / "Greater X Area" formats.
    2. Substack extraction: WebFetch /about page; parse author bio for "Based in X" / "Living in Y" / explicit location mentions.
    3. Normalize to format above using country/state abbreviation lookup tables (state mapping for US 50 + DC + territories; subdivision mapping for top 10 non-US countries).
    4. Add optional `location_filter` parameter to skill input (default ["US"]; accepts list of country abbreviations) — filter candidates post-extraction.

  Deferred from v1.0 because LinkedIn search snippets don't reliably surface location and Substack location is bio-text that needs page-fetch + parse. v1.0 left Location column blank; Luis manually populated during review.

  Capture as v1.1 build alongside: Reddit PRAW integration, Audience Size population, "exactly the kind of" template echo fix.
- 2026-05-07: psitta-outreach-research v1.1 production run produced 64 candidates with only 7 Location values populated; 57 marked Unknown. Skill chose not to per-candidate WebFetch each profile to extract location (would have added 5-15 min runtime). For v1.2: make per-candidate WebFetch the default behavior in Phase B (acceptable runtime cost given Location is workflow-critical). Alternative for v1.2: location_extraction parameter (default "thorough" = WebFetch all; "fast" = snippet-only as v1.1). Bug also caught mid-run: normalize_location.py had "ca" lowercase 2-letter alias for Canada that collided with US state CA (e.g. "Solana Beach, CA" → "Canada-Solana Beach"). Fixed by removing the lowercase 2-letter "ca" alias; full-word "canada" preserved. Self-test re-run PASS.
- 2026-05-07: Cognito Pre-Token-Gen v2 architecture -- Cognito access tokens by default OMIT user-attribute claims (incl. email). ID tokens contain email by default if scope present. Desktop apps send access token (not ID token) to API per OAuth/OIDC convention. Pre-Token-Gen v2 Lambda is the AWS-recommended way to inject custom claims into access tokens.
- 2026-05-07: Cognito Pre-Token-Gen v2 architecture -- V2 schema requires explicit lambda_version = "V2_0" in Terraform pre_token_generation_config block. V1 only modifies ID tokens; only V2 modifies access tokens. Single most critical config line in Stage 1.
- 2026-05-07: Cognito Pre-Token-Gen v2 architecture -- AWS Provider 5.x adds default attributes to existing resources causing routine terraform plan -refresh-only drift. S3 SSE bucket_key_enabled, CloudFront trusted_key_groups[]/trusted_signers[]/whitelisted_names[], Route53 records=[] are all benign provider-version-drift. Audit each before reconciling.
- 2026-05-07: ECS production migration pattern -- ECS one-off task pattern for production migrations: aws ecs run-task --cluster psitta-cluster --task-definition psitta-api --launch-type FARGATE --network-configuration awsvpcConfiguration={subnets=[subnet-0a143e23d5e240aa9,subnet-0653bdf3529d8bbe8],securityGroups=[sg-002cf129761af804f],assignPublicIp=DISABLED} --overrides '{"containerOverrides":[{"name":"psitta-api","command":["alembic","upgrade","head"]}]}'. Image must include migration file BEFORE run-task fires (commit → push → CI build → ECR digest match → run-task).
- 2026-05-07: Migration safety net pattern -- 3 pre-flight assertions (orphan exists, doc count matches diagnostic, prefix matches expected count) + 3 post-flight assertions (zero rows match anti-pattern, zero rows still have target sub, expected emails present in users.email) + transaction rollback on RuntimeError = 5-deep safety with deterministic abort on state shift. Cheap defense; no operational cost; catches state drift between diagnostic and apply.
- 2026-05-07: psitta-outreach-research architecture is reusable -- Generic components (build_xlsx.py with USER_EDITED_FIELDS preservation + auto-backup; normalize_location.py country/state lookups; Phase A→E protocol; workflow tracking columns) reuse as-is for any future founder-led outreach product. Per-product customization limited to: 7 template files (voice + value prop + install URL + tester perks), voice_profile.md if founder voice changes, target_profile defaults. Fork time estimate: 1-2 hours per new product.
- 2026-05-07: Reddit blocked at Anthropic crawler level -- Discovered during first skill invocation — site-owner-controlled domain block on reddit.com for both web_search and web_fetch. Workarounds: PRAW (proper API path with Reddit OAuth, ~30-60 min build), manual research with skill drafting only, or skip. Reddit subreddit no-promo rules also stricter than LinkedIn/Substack — double-difficult.
- 2026-05-07: /review vs /ultrareview calibration -- /review (local-branch-vs-main, fast/free) preferred over /ultrareview (cloud-based, 3 free runs, full PR diff) for routine production code, schema migrations <100 lines, dev tooling. /ultrareview reserves remaining free-tier budget for highest-stakes changes (Stage 4 schema rename, Items 9/10 implementation, payment changes). Skip review entirely for docs/config/tests.
- 2026-05-07: Phase-gate protocol calibration -- Phase A approval gates valuable for: production database migrations, IAM/auth changes, anything irreversible touching prod, AWS-cost-incurring operations. Phase A gates are theater for: research-only skills with auto-backup, local-file generation, documentation builds, dev-tooling changes. For psitta-outreach-research and similar research/draft skills, run continuously through phases without intermediate gates.
- 2026-05-07: v1.2 backlog from v1.1 production run -- Per-candidate WebFetch in Phase B for Location auto-fill (currently 7 of 64 populated; 57 Unknown). 5-15 min runtime cost worth it given Location is workflow-critical.
- 2026-05-07: v1.2 backlog from v1.1 production run -- PRAW Reddit integration if Reddit candidates become essential.
- 2026-05-07: v1.2 backlog from v1.1 production run -- Audience Size column auto-population (LinkedIn follower count where visible; Substack subscriber count from public profile).
- 2026-05-07: v1.2 backlog from v1.1 production run -- Template B's canonical "exactly the kind of" phrase echoes when [specific compliment] also contains it. Either rewrite Template B's second sentence or add a voice-rule preventing the phrase in [specific compliment] field.
- 2026-05-08: v1.0.6.0 first external-distribution MSIX shipped end-to-end. Six surfaces touched atomically: pubspec.yaml, MSIX build, S3 versioned path (releases/1.0.6.0/), .appinstaller XML at bucket root, CloudFront invalidations on both /psitta.appinstaller AND /releases/1.0.6.0/psitta.msix, website download page label. Initial publish broke because .msix was uploaded to bucket root instead of versioned path AND .appinstaller XML was not regenerated — App Installer dialog showed v1.0.5.0 despite local v1.0.6.0 build. Fixed by re-uploading .msix to releases/1.0.6.0/, regenerating .appinstaller (3 version field edits, MainPackage Uri update), invalidating both CloudFront paths, deleting stray bucket-root .msix. Lesson encoded for psitta-version-update skill (deferred build).
- 2026-05-08: v1.0.6.0 first external-distribution MSIX shipped end-to-end after recovery from publish-path failure. Lesson: a Psitta version update touches six surfaces atomically — pubspec.yaml, MSIX build, S3 versioned path, .appinstaller XML at bucket root, CloudFront invalidations on both paths, website download page label. Touching only some produces a release that 'looks built but ships old bytes.'
- 2026-05-08: The .appinstaller XML is the source of truth for what Windows App Installer displays. Even if the new .msix is uploaded correctly, if the .appinstaller still references the old version (root <AppInstaller Version>, <MainPackage Version>, <MainPackage Uri> — three places), Windows installs the old bytes. Always regenerate the XML; never assume the .msix upload alone is sufficient.
- 2026-05-08: CloudFront invalidation must cover both paths — /psitta.appinstaller (60s TTL but still cached at edges) AND /releases/<version>/psitta.msix (1y TTL — definitely cached). Single-path invalidation of just /psitta.msix does nothing useful; the .msix lives at the versioned path, not bucket root.
- 2026-05-08: S3 uploads must specify --content-type application/msix for .msix files and --content-type application/appinstaller for .appinstaller. Default Content-Type detection picks application/octet-stream which Windows App Installer may reject or treat ambiguously.
- 2026-05-08: The psitta-dev.pfx self-signed certificate that ships v1.0.6.0 will cause every fresh tester machine to display Publisher: Unknown and disable the Install button (Windows error 0x800B010A — root cert not trusted). Luis's machine works because the dev cert is in his Trusted Root from earlier testing. Daonet's first install attempt confirmed this in the wild on May 9 morning. Procurement of a public-CA OV (Organization Validation) code signing certificate (SSL.com or Sectigo) is required before alpha distribution can proceed at scale. EV (Extended Validation) cert is the only path to instant SmartScreen reputation but requires hardware token + 5-10 day vetting; OV is the right move for alpha (24h vetting at SSL.com).
- 2026-05-08: AWS CLI from Claude Code's Bash subprocess fails SSL validation against AWS STS endpoints while AWS CLI from interactive PowerShell on the same machine works clean. Symptom: SSL: CERTIFICATE_VERIFY_FAILED unable to get local issuer certificate. Workarounds (AWS_CA_BUNDLE env var, --ca-bundle flag inline) did not resolve. Likely root cause: Bash subprocess resolves aws to a different binary than PowerShell — possibly a Python pip-installed awscli v1 with stale internal cert chain — instead of /c/Program Files/Amazon/AWSCLIV2/aws.exe v2.34.5. Diagnostic deferred to May 9.
- 2026-05-08: Git Bash on Windows (MSYS) auto-converts forward-slash arguments that look like Unix paths into Windows paths. aws logs tail --log-group-name /ecs/psitta-api becomes C:/Program Files/Git/ecs/psitta-api. Prefix the command with MSYS_NO_PATHCONV=1 or export MSYS_NO_PATHCONV=1 for the session.
- 2026-05-08: ECS one-off task pattern for tester_allowlist operations is the durable interface: aws ecs run-task with override 'python scripts/grant_tester.py <subcommand> <args>'. Use aws logs filter-log-events --start-time = task.createdAt - 60s for retrieval rather than aws logs tail (which is timing-fragile and frequently misses the stream attach window). Email addresses are lowercased on insert per script convention.
- 2026-05-08: Disabling SSL verification (verify=False) for assume_role with admin credentials (OrganizationAccountAccessRole on cross-account) is a hard security violation regardless of how convenient the unblock — TLS interception in transit could capture the assumed credentials. Saved as durable rule. Workaround was rejected and proper certifi path pursued instead.
- 2026-05-08: Pre-MSIX security gate verdict structure (10 sections, source-level + runtime, GREEN/YELLOW/RED) is reusable for any pre-release gate. Today's gate identified two real findings (M1 security headers, L5 raw emails in structlog) and confirmed yesterday's auth Stage 1+2 work was clean from an attack-surface perspective.
- 2026-05-09: Norton HTTPS Scanning ('Encrypted Connections Scanning') was the root cause of two SSL failures: (1) the May 8 AWS CLI Bash subprocess wall — CERTIFICATE_VERIFY_FAILED from Git Bash, and (2) today's AdLibrary API discovery wall — both CRYPT_E_NO_REVOCATION_CHECK from curl Schannel and 'Basic Constraints not marked critical' from Python 3.13 urllib. Norton intercepts outbound HTTPS, decrypts traffic, re-encrypts with its own cert chain — and that injected intermediate has a non-critical Basic Constraints extension which violates RFC 5280 strict validation. Browsers softly tolerate this; CLI tools (curl, Python) reject it. Fix: Norton Settings → exclude curl.exe + python.exe from HTTPS scanning. Diagnostic-first protocol: always probe a known-good HTTPS site (Google) when AWS or anything else fails — same-symptom across hosts = environment-level cause, not site-specific.
- 2026-05-09: AdLibrary.com Business plan API access provisioned. API key 'psitta-ad-search' stored at /c/products/psitta/.env as ADLIBRARY_API_KEY (gitignored). Skill scaffold created at .claude/skills/psitta-ad-search/. Phase A discovery blocked on AdLibrary's /api/search returning HTTP 500 with empty body — confirmed as their origin failure (Cloudflare CF-Cache-Status DYNAMIC + Next.js App Router Vary headers indicate unhandled server exception). Both Speechify and their own canonical 'fitness' example 500 identically. Failed calls don't bill (Usage tab confirms 0 credits consumed). Skill paused pending AdLibrary recovery; resumption is one curl probe + one Phase A re-run prompt.
- 2026-05-09: AdLibrary's /api/enrichment endpoint replaces our planned own-LLM enrichment in Phase C of the psitta-ad-search skill. Returns structured: summary, transcription (scene-by-scene with Visual+Speech for video ads), analysis (Hook Forensics), ugc_script (1:1 UGC replication brief), markdown (full doc), source. Cached 30 days per ad. Locks Phase C from 'build LLM orchestration' to 'single API call' — drops skill build estimate from 3hr to 2hr and adds the UGC script as a competitive-intelligence asset we hadn't planned.
- 2026-05-09: When a HTTPS client fails on a target (AdLibrary, AWS, etc.), the first diagnostic move is probing 2-3 known-good HTTPS sites (Google, GitHub, AWS) before assuming the target is at fault. Same-symptom failure across multiple targets indicates environment-level cause (proxy, antivirus, DNS, network); single-target failure indicates target-specific issue. This protocol surfaced Norton HTTPS Scanning as the May 8 + May 9 root cause in under 10 minutes — vs. potentially hours of guessing at AdLibrary cert configuration.
- 2026-05-09: Bash 'read -rs' (silent input) for secret paste in Git Bash on Windows is unreliable: the silent-mode design provides no visual confirmation that paste landed in the buffer, and right-click paste behavior in Git Bash MinTTY can be inconsistent. Reliable fallback: open .env in VS Code, manual paste of new line, save with Ctrl+S, close tab. Verification by length-only awk: 'awk -F= /^ADLIBRARY_API_KEY=/ {print length($2)}' confirms paste landed without ever displaying or logging the value. Always create .env.bak_<timestamp> before modification. Always confirm .gitignore excludes .env BEFORE writing the secret.
- 2026-05-09: AdLibrary's billing system does NOT charge credits for failed API calls. A POST /api/search that returns HTTP 500 logs in the Usage tab with Status 500 and Credits 0. This means safe to retry/diagnose against their API without burn risk on transient or origin-side failures. Their billing only deducts on 200-status responses.
- 2026-05-10: Python stdlib urllib.request defaults the User-Agent header to "Python-urllib/3.12", which Cloudflare's bot-management treats as a banned signature (error_code 1010, browser_signature_banned) and blocks BEFORE the request reaches Resend's API. Any Lambda calling api.resend.com with stdlib MUST set an explicit User-Agent header (e.g., "psitta-welcome-email/1.0 (+https://psitta.ai)"). Symptoms: HTTP 403 from Cloudflare, not Resend; identifiable by error_code: 1010 in the response body. Fix: 3-line urllib.request.Request headers dict update.
- 2026-05-10: Claude Code's bash session and operator's separate Git Bash terminal can be authenticated against different AWS accounts. Operator's default profile may be Blowmymind (010526248733); Psitta-prod operations require export AWS_PROFILE=psitta-prod (assumes OrganizationAccountAccessRole). Always run aws sts get-caller-identity --no-cli-pager in operator terminal before secret/Secrets Manager operations. Diagnostic for ResourceNotFoundException on a secret that was just created by Claude Code: the secret exists in 808765744063 but the operator terminal is hitting 010526248733.
- 2026-05-10: Windows file-system lock on terraform.tfstate can outlast the terraform process. After confirming tasklist //FI "IMAGENAME eq terraform.exe" returns no running process, it is safe to bypass with terraform apply -lock=false. Distinct from Terraform's own .terraform.tfstate.lock.info lock-info file. Lock typically releases within seconds, but can persist longer if Norton, OneDrive sync, or another file-handle holder is touching the .tfstate.
- 2026-05-10: Per AdLibrary support (Murat, founder, email 2026-05-10): appType=2 is the legacy in-app-ads bucket, being deprecated due to low usage and currently in rework. Migration to appType=3 recommended for future-proofing. First skill run with appType=2 successfully captured 230 ads across 12 keywords but underperformed expectations on direct competitors (Speechify: 1g/1s, Blinkist: 1g/0s) — appType=2 is filtering their primary creative inventory. Supplementary appType=3 run flagged for follow-up (12 credits, ~5 min). SKILL.md for psitta-ad-search captures this calibration note.
- 2026-05-11: What looks like deliberate anti-scraping behavior may actually be a broader robots/bot-block configuration that the operator hasn't deliberately scoped to authenticated sessions. Symptom seen on AdLibrary 2026-05-10: media plays ~1 second via direct /api/media URL when accessed from a logged-in browser, then video stream cuts to black while audio continues. Resolution path: ask the API provider directly rather than designing around assumed restrictions. AdLibrary founder Murat confirmed (2026-05-11) the block was unintentional and committed to removing it for authenticated direct-file access. Lesson: 1 well-framed email to the API provider can save hours of workaround engineering.
- 2026-05-12: KL 2026-05-12 - VPC Lambda needs explicit network path to AWS service APIs -- A Lambda placed in VPC private subnets has NO internet access by default and cannot reach secretsmanager.<region>.amazonaws.com, api.resend.com, or any other AWS service endpoint until you add either a NAT Gateway (with route from private subnet) or a VPC Interface Endpoint per service. Symptom is a 30-second hang with no logs after the handler's first log line. The hang is the boto3 SDK retrying against an unreachable endpoint. Diagnose with aws ec2 describe-vpc-endpoints to enumerate what exists. If endpoints exist, verify their security group ingress allows source from your Lambda's SG via aws ec2 describe-security-groups - this is a second hidden failure mode (endpoint exists, traffic blocked at SG).
- 2026-05-12: KL 2026-05-12 - RDS IAM auth requires TWO flags + SSL + IAM token -- Lambda → RDS via IAM authentication requires ALL of: (1) RDS instance-level iam_database_authentication_enabled=true, (2) PostgreSQL-side GRANT rds_iam TO <user>, (3) psycopg2.connect() with sslmode='require', (4) password parameter set to a fresh IAM token from boto3.client('rds').generate_db_auth_token(). If flag (1) is missing but everything else is right, Postgres rejects with the misleading "password authentication failed" error, NOT "IAM auth disabled." Diagnose with aws rds describe-db-instances and check IAMDatabaseAuthenticationEnabled. If it's false but PendingModifiedValues shows the change queued, hit it with --apply-immediately to skip the maintenance window.
- 2026-05-12: KL 2026-05-12 - Terraform apply_immediately defaults to false for RDS modifications -- When you flip iam_database_authentication_enabled = true (or any RDS attribute) in Terraform, the default behavior is to queue the change for the next maintenance window. Terraform reports "Apply complete" but AWS leaves the actual flag unchanged with PendingModifiedValues populated. Force immediate apply via aws rds modify-db-instance --apply-immediately CLI override. Best practice: leave apply_immediately = false in Terraform (so engine version upgrades aren't surprise-applied) but override via CLI for one-off changes that need to land now.
- 2026-05-12: KL 2026-05-12 - AWS SG description charset is narrower than ASCII -- Security group descriptions and ingress-rule descriptions accept only: a-zA-Z0-9. _-:/()#,@[]+=&;{}!$*. ASCII characters REJECTED include > < " ' ` ~ ^ % \ | ?. Common offenders: -> arrows in descriptions ("Lambda -> RDS"), backticks in code-like strings, percent signs. Lint Terraform SG/rule descriptions against this charset, not just non-ASCII. Em-dashes (—) are a separate category - rejected at the non-ASCII level.
- 2026-05-12: KL 2026-05-12 - Mixing inline ingress with separate ingress_rule resources causes silent SG churn -- If aws_security_group.X has an inline ingress {} block AND you add separate aws_vpc_security_group_ingress_rule resources targeting the same SG, every terraform apply silently deletes the separate-resource rules during SG reconciliation. Diff doesn't show this clearly. Fix: pick ONE pattern per SG. If migrating from inline to separate, remove the inline block AND import the existing rule via terraform import - do NOT recreate (Authorize will fail with InvalidPermission.Duplicate). Modern AWS provider 5.x prefers separate aws_vpc_security_group_ingress_rule resources for granular control.
- 2026-05-12: KL 2026-05-12 - VPC Lambda stale ENI after VPC-affecting modifications -- After modifying VPC-adjacent infrastructure (RDS endpoint changes, subnet route table updates, security group rules), a VPC Lambda may hold a stale ENI that doesn't reflect the new networking state. Symptom: Lambda invokes timeout connecting to internal services that other workloads can reach. Resolution: wait 10-15 min for ENI lifecycle to release the stale ENI back to the VPC pool, OR force redeploy the Lambda function (touch source code to trigger archive_file hash change → terraform apply). The ENI gets refreshed on cold start. This is what hit us today - the IAM auth flag flip on the RDS instance triggered an internal endpoint change that the Lambda's cached ENI didn't see; after ~30 min idle the natural cycle resolved it.
- 2026-05-12: KL 2026-05-12 - Root user disabled in member accounts per AWS Organizations centralize-root policy -- When AWS Organizations is configured with the "Centralize root access" security policy, root user sign-in is DISABLED for member accounts. Attempting to sign in shows: "Your root user credentials are not valid or sign-in as root is currently disabled for this account." Direct console access to member accounts requires either: (a) Switch Role from an IAM user in the management account that has sts:AssumeRole permission on the target role (BUT root users of the management account cannot Switch Role via browser - must use IAM user, NOT root), or (b) sign in directly to the member account using an IAM user that exists there. Bookmark the Switch Role URL: https://signin.aws.amazon.com/switchrole?account=<member-account>&roleName=<role>&displayName=<friendly-name>.
- 2026-05-12: KL 2026-05-12 - Capture cross-session knowledge as durable SKILL.md, not just KL entries -- When a debugging session surfaces multiple discrete gotchas in a related domain (e.g., today's 8 VPC-Lambda-related gotchas), the value of a dedicated SKILL.md in .claude/skills/<topic>/ is far higher than scattered KL entries in CLAUDE.md. SKILL.md provides: frontmatter for skill-loader auto-discovery, structured sections with diagnostic + fix patterns, canonical code structures, build checklists, cleanup runbooks. KL entries in CLAUDE.md remain valuable as the immutable append-only log of "what bit us when" - the two artifacts complement, they don't substitute. Pattern: every gotcha gets a 1-paragraph KL in CLAUDE.md, and gotcha clusters of 3+ around a single domain get a dedicated SKILL.md.
- 2026-05-15: KL 2026-05-15 — AdLibrary bot-block removal verified propagated. 14-ad stratified sweep across all 3 platforms (admob/facebook/instagram), both media kinds (video/image), and both tiers (gold/silver) returned 14/14 HTTP 200 with valid MP4/JPEG content. Two operational quirks documented in psitta-ad-search SKILL.md: HEAD returns 404 (use GET), Content-Length absent (chunked encoding, must fetch body). Marketing/launch handoff unblocked.
- 2026-05-15: KL 2026-05-15 — SSL.com CodeSignTool does NOT sign MSIX at any version -- CodeSignTool (the Java-based SSL.com CLI tool) supports exe, dll, msi, cab, ocx, scr, sys, ps1, ps1xml, vbs, js, wsf, jar, nupkg, ovf, ova. MSIX, MDB, and ACCDE are supported ONLY by eSigner CKA (the Windows Cloud Key Adapter that bridges eSigner Cloud certs into Windows cert store for signtool.exe). When the deliverable is MSIX, skip CodeSignTool entirely — go directly to CKA + signtool. Source of truth: https://www.ssl.com/guide/esigner-supported-file-types/. This tripped tonight because a prior Claude session pointed at CodeSignTool without flagging the MSIX gap.
- 2026-05-15: KL 2026-05-15 — Norton HTTPS Scanning breaks Schannel revocation checks across the entire toolchain -- Norton HTTPS Scanning intercepts outbound 443 and sits between Schannel and the real OCSP/CRL responders. Schannel cannot complete revocation lookups and fails with curl: (35) CRYPT_E_NO_REVOCATION_CHECK. This affects not just curl but every libcurl-Schannel-based downloader on the host, including CMake's file(DOWNLOAD). When pdfrx's CMakeLists.txt does an unguarded file(DOWNLOAD) without STATUS or EXPECTED_HASH, the failure is silent and produces a 0-byte file, which then causes the file(COPY) at lines 59/64 to fail with the misleading message "cannot find ... File exists" (the "File exists" suffix is the OS errno, not a positive observation). Recovery: disable Norton HTTPS Scanning. Long-term: any CMake-based pipeline on this host needs to be reviewed for unguarded downloads.
- 2026-05-15: KL 2026-05-15 — Human-escalated SSL.com cert issuance may not auto-bind to eSigner user -- When an OV Code Signing cert is issued via SSL.com's human escalation path (Amal-style) rather than self-service, the cert may be issued and enrolled in eSigner Cloud but NOT bound to a specific eSigner user account. The cert is visible in the portal (with full eSigner ID, credential serial, label) but the eSignerKSP provider never enumerates it because the binding is missing. Diagnostic command: certutil -csp "eSignerKSP" -key — enumerates only credentials bound to the current authenticated user. If the expected credential is absent from this output despite being visible in the portal, the issue is backend user-binding, not local KSP/CKA. Remediation requires SSL.com backend engineering ticket; the customer cannot fix it via portal UI.
- 2026-05-15: KL 2026-05-15 — curl reachability tests must avoid -s and piped head -- For diagnostic curl invocations testing whether a remote URL is reachable, never combine -s (silent mode) with a pipe through head/tail/grep. -s suppresses curl's error messages; the pipe captures the piped command's exit code instead of curl's. The combined effect is empty output that cannot distinguish 404 from TLS failure from connection error. Correct invocation: curl -ILv -A "Mozilla/5.0" <URL> 2>&1; echo "CURL_RC=$?" — full verbose trace, real exit code surfaced. For control comparisons, use ${PIPESTATUS[0]} to capture the first pipeline element's exit code when piping is unavoidable.
- 2026-05-16: Git Bash 'ls -F' alias mangles signtool path capture. Use 'command ls -1 ...' to bypass shell aliases when locating Windows tools from Bash.
- 2026-05-16: signtool path arguments require MSYS_NO_PATHCONV=1 + Windows-native paths in Git Bash. MSYS path conversion mangles /v, /fd, /tr, /td, /sha1 flags and breaks the entire command.
- 2026-05-16: curl -I sends HEAD; do not use against GET-only endpoints. /api/v1/health is GET-only and returns 405 to HEAD. Use 'curl -v' without -I for verb-correct health probes.
- 2026-05-16: SSL.com CKA 'Install Certificate...' button (via Windows Certificate Import Wizard) is REQUIRED to persist the cert in Current User\Personal — opening and closing CKA without clicking Install causes the cert to disappear from the local store. (Correction to my morning advice on this point.)
- 2026-05-16: SSL.com human-escalated cert issuance may not establish operational eSigner credentialID binding, AND the binding may regress even after a partial backend fix. Reboot does not remediate. Reaffirms and strengthens KL 2026-05-15.
- 2026-05-18: Two independent Microsoft-shipped tools exhibiting identical KSP-mediated failure modes against the same credential is conclusive evidence of vendor-side backend failure, not local config drift. certutil -repairstore and signtool both failing identically on the same eSigner KSP-bound credential isolates the failure exclusively to the KSP→backend round-trip.
- 2026-05-18: Since Microsoft's March 2024 SmartScreen update, NO code signing cert tier — OV or EV from any CA — bypasses SmartScreen warnings on first run. Reputation is built through download volume. Launch UX MUST pre-warn users about 'Windows protected your PC' notices regardless of cert choice. EV's instant-bypass advantage was removed.
- 2026-05-18: Microsoft-Windows-CAPI2/Operational event log is disabled by default on Windows. For any production signtool/CryptoAPI troubleshooting it MUST be explicitly enabled BEFORE the failing attempt via wevtutil sl in an elevated terminal — retroactive extraction is impossible because no events were captured when the log was disabled. Enable proactively on any dev machine that will run signtool against a cloud signing service.
- 2026-05-19: Append-only, date-prefixed, per CLAUDE.md protocol. Do not overwrite prior entries.
- 2026-05-19: KL 2026-05-19a — Stripe webhook URL must carry the /api/v1 prefix. The live webhook was registered as /billing/webhook and 404’d every event; the real route is /api/v1/billing/webhook. Symptom was “paid but tier never changed.” Diagnosis path: Stripe Health → failed-delivery HTTP status. 404 = wrong path (not signature). Fix is a Dashboard config edit + Resend; no deploy. This same URL governs ALL billing events.
- 2026-05-19: KL 2026-05-19b — Billing maps by psitta_user_id metadata, not email. Stripe customer metadata carries psitta_user_id (backend UUID). Webhook handler resolves the user by this UUID. Therefore synthetic auth0.local emails (U1/U2 tech debt) do NOT break billing. Confirmed during live E2E.
- 2026-05-19: KL 2026-05-19c — Portal cancel defaults to cancel-at-period-end. Customer Portal cancellation sets cancel_at_period_end=true and fires customer.subscription.updated; the user keeps Pro until period end and the app correctly continues showing Pro. customer.subscription.deleted only fires at period end. To test the deletion/downgrade path immediately, cancel from the Dashboard with “Cancel now.” Real-time downgrade verified (no re-login needed).
- 2026-05-22: Append-only, date-prefixed.
- 2026-05-22: KL 2026-05-22a — Sandbox stripe_customers rows survive the live cutover and 502 on any live Stripe call. The May 19 sandbox→live cutover left pre-cutover cus_ IDs unresolvable by the live API (separate object stores). Symptom: “No such customer” on portal-session. Same class as the May 19 webhook URL bug — a live-cutover artifact. Live cutovers require auditing pre-cutover customer rows.
- 2026-05-22: KL 2026-05-22b — Gate “Manage Subscription” on subscription SOURCE, not Pro status. The portal only works for users backed by a live Stripe customer. Comp/grandfathered and allowlist/grant users have Pro but no Stripe customer, so they 502. The app already hides the button for Free; extend that to hide it for non-Stripe-paid Pro.
- 2026-05-22: KL 2026-05-22c — Client-side timeouts must account for Lambda cold-start tail latency. tester-digest had an 8s Resend HTTP timeout and 10s RDS connect timeout — both too tight on cold start. Raised to 20s/15s (Lambda budget is 30s). Set external-dependency timeouts with cold-start headroom.
- 2026-05-22: KL 2026-05-22d — Diagnose → fix → THEN instrument. Never alarm a signal you don’t yet understand; alarming a known-noisy/known-broken signal trains you to ignore the alert. Applied to the digest (alarm deferred until baseline re-measured) and the portal (P3-11 portal metric deferred until the gating fix lands).
- 2026-05-22: KL 2026-05-22e — “Passed verification” means little until probed. The hook parser revealed 4 hidden flaws and the May 19 webhook had silently 404’d — both had “passed.” Re-run full verification tables on parser/automation changes; build observability before stacking more unattended automation.
- 2026-05-23: 2026-05-23a — Sandbox stripe_customers rows survive a live cutover and 502 on any live Stripe call (same class as the webhook-URL bug). Live cutovers require auditing pre-cutover customer rows.
- 2026-05-23: 2026-05-23b — Gate “Manage Subscription” on subscription SOURCE (source=stripe), not Pro status — comp / allowlist / dev_override users have no Stripe customer.
- 2026-05-23: 2026-05-23c — Prove the happy path, not just the fix: the button-hides case was easy with existing accounts; the button-shows-and-portal-opens case required a real live paid signup because all owned accounts were sandbox-era.
- 2026-05-23: 2026-05-23d — SNS confirmation_timeout_in_minutes / endpoint_auto_confirms are creation-time-only schema attributes, left null in state after import. Explicit declaration causes a permanent diff; use lifecycle.ignore_changes. Import-not-recreate; success criterion = no-op plan.
- 2026-05-23: 2026-05-23e — Run terraform plan BEFORE committing the codification, not on the expectation of a clean plan — the first plan caught a still-drifting SNS subscription and the separate tester-digest drift that would otherwise have ridden along.
- 2026-05-24: 2026-05-24a — certutil -store emits 'Could not verify certificate public key against private key' for eSigner cloud-KSP certs as EXPECTED/BENIGN behavior (private key is remote/non-exportable). NOT a binding regression. The authoritative health test is signtool sign itself, not certutil. Distinct from the May-18 signing failure.
- 2026-05-24: 2026-05-24b — The production .appinstaller OnLaunch block carries UpdateBlocksActivation="false" + ShowPrompt="false" beyond the documented schema (silent auto-update). Always dump the LIVE manifest before authoring a new version; only 3 fields change per release (2 Version attrs + MainPackage Uri path).
- 2026-05-24: 2026-05-24c — MSIX release ordering: upload the .msix to the versioned path BEFORE flipping the manifest, so the manifest never references a missing file. Set MIME at upload (application/msix, application/appinstaller) — wrong MIME silently breaks App Installer fetches. CloudFront invalidate the manifest path (60s TTL) to go live; verify with a live curl (X-Cache Miss = fresh).
- 2026-05-24: 2026-05-24d — SWH does NOT drive premium quota — voice choice does. SWH-always burns identical quota to SWH-never. Subtitle copy must describe behavior, not warn about a cost SWH doesn't cause.
- 2026-05-24: 2026-05-24e — Auto-update reaches installed testers on app LAUNCH (OnLaunch), not on login; may apply on the second relaunch. The flutter run dev build is NOT tied to the .appinstaller and never auto-updates.
- 2026-05-24: Append to CLAUDE.md Key Learnings (append-only, date-prefixed):
- 2026-05-25: Append to CLAUDE.md Key Learnings (append-only, date-prefixed):
- 2026-05-25: 2026-05-25a — Console Switch-Role into psitta-prod fails from the Blowmymind mgmt account (luis-admin exists only there; switch-role-from-root unreliable). PERMANENT FIX: native IAM admin user luis-admin created directly inside psitta-prod (808765744063); login at https://808765744063.signin.aws.amazon.com/console. The two luis-admin users (one per account) are independent. TODO: enable MFA on the prod user.
- 2026-05-25: 2026-05-25b — Ground 0 baseline: ~1 genuine external user (Timothy); rest are founder/family/test. 0 synthetic @auth0.local emails (v1.1 concern moot). Validates marketing pivot.
- 2026-05-25: 2026-05-25c — Download/install logging is OFF (CloudFront + S3). No clean historical download count. The ~1,330/30d CloudFront Requests figure is mostly .appinstaller manifest polls, not downloads. PREREQ before marketing: enable CloudFront standard access logging to a new S3 bucket (~$0.001-0.05/mo), then query per-object/per-version via Athena. Installs (local MSIX action) may be unmeasurable regardless.
- 2026-05-25: 2026-05-25d — Business-metrics dashboard does NOT exist (Psitta-Automation is infra-health only). Recommended build: a snapshot tool querying Postgres+Stripe+Cognito directly, NOT CloudWatch custom metrics (which would require instrumenting every business event and only capture data going forward). Queued as a milestone, not built.
- 2026-05-26: 2026-05-26a — Plausible’s new per-site script (Oct 2025) encodes the domain in the pa-<id>.js filename — no data-domain attribute. Optional measurements (outbound links, file downloads, form submissions) are server-compiled dashboard toggles and activate without a redeploy.
- 2026-05-26: 2026-05-26b — Plausible treats file links on a different subdomain (download.psitta.ai vs psitta.ai) or CDN as Outbound Link clicks, NOT File Downloads. For cross-subdomain download tracking, enable “Outbound links.” Note: .appinstaller / .msix are not in Plausible’s default file-download extension list anyway.
- 2026-05-26: 2026-05-26c — Next.js static export cannot mint per-request CSP nonces, and hydration-chunk hashes vary per build — so script-src/style-src 'unsafe-inline' is mandatory. The CSP remains valuable: it still locks default-src, object-src, frame embedding, and the connect allowlist.
- 2026-05-26: 2026-05-26d — Cloudflare Pages security headers ship via website/public/_headers (copied to out/_headers on build). next.config headers() is a no-op in static export — do not use it.
- 2026-05-26: 2026-05-26e — Cloudflare SSL/TLS “Flexible” mode leaves the Cloudflare→origin leg unencrypted. A green padlock / https in the browser is NOT proof the mode is correct. For Cloudflare Pages, use Full (strict).
- 2026-05-26: 2026-05-26f — HSTS preload is effectively irreversible and forces all subdomains to HTTPS — default to includeSubDomains WITHOUT preload until certain. Use Referrer-Policy strict-origin-when-cross-origin (not no-referrer) to preserve cross-property referral attribution in analytics.
- 2026-05-26: 2026-05-26g — Cloudflare Pages deploy/propagation: a curl run immediately after push often shows stale or partial results — wait ~90 seconds before concluding anything is broken. Auto-deploy on push-to-main for the facti-ai-website Pages project is confirmed wired.
- 2026-05-29: Append-only, date-prefixed. Candidates for the CLAUDE.md Key Learnings file.
- 2026-05-29: 2026-05-29a — Terraform native S3 locking (use_lockfile = true) works on Terraform 1.14.6 — no DynamoDB lock table needed. The lock object (…/terraform.tfstate.tflock) is created on acquire and deleted on release; seeing it as a non-latest version with a delete marker on top confirms a clean acquire/release cycle, not a stuck lock.
- 2026-05-29: 2026-05-29b — Norton HTTPS Scanning intercepts outbound 443 from Claude Code's Bash subprocess and injects a cert chain that AWS CLI / urllib reject (CERTIFICATE_VERIFY_FAILED) — but the operator's own Git Bash shell is unaffected. Run AWS/terraform from your own shell, OR toggle Norton HTTPS Scanning off for Claude Code execution and back on after. NEVER weaken SSL (no --no-verify-ssl, no AWS_CA_BUNDLE override).
- 2026-05-29: 2026-05-29c — terraform init -migrate-state (local → S3) empties the local primary terraform.tfstate to 0 bytes and writes the full pre-migration state into terraform.tfstate.backup. The 0-byte primary file is expected, NOT data loss — the live state is in S3 and a full local backup is preserved.
- 2026-05-29: 2026-05-29d — var.rds_dbi_resource_id was operator-supplied via -var at apply time and never persisted to terraform.tfvars, causing terraform plan to block on an interactive prompt. Fix: append the value (db-2SKPU3IOC2MLTPKVEEHS7NIQUQ, a non-secret AWS resource ID) to tfvars. Cleaner future fix: reference aws_db_instance.main.dbi_resource_id directly and drop the variable.
- 2026-05-29: 2026-05-29e — KYCShield naming collision: C:\python\facti-ai\ is the LEGACY KYCShield monorepo, NOT the Facti AI website (which is C:\products\facti-ai\). Same leaf name, different products. Always confirm by contents, not folder name.
- 2026-05-29: 2026-05-29f — Trusi's VerifAI → Trusi rebrand is incomplete: pyproject.toml (verifai-backend), Docker containers (verifai-postgres/redis), Makefile header, and — critically — mobile/src/config/constants.ts which still has APP_NAME='VerifAI' and a production URL of https://api.verifai.com (a domain that isn't owned). The constants.ts production URL is a real bug, not just cosmetic.
- 2026-05-29: 2026-05-29g — Cloud providers (AWS, Route 53, Cloudflare) give DURABILITY (hardware-failure protection) but not RECOVERABILITY against operator error, account compromise, or region outage. Configuration backups (Terraform state, Route 53 / Cloudflare zone exports), beyond-default data retention, and account-recovery paths are the customer's responsibility — they belong in the backup plan.
- 2026-05-29: 2026-05-29h — Plaintext-secrets sprawl on the laptop: terraform.tfvars holds 5 plaintext secrets; every terraform state file (live + the 5+ stray local .bak snapshots in the infra dir) holds plaintext secret material. Migrating state to encrypted S3 addresses the live copy; the tfvars + stray local backups remain a confidentiality gap for the Session 3 secrets cleanup.
- 2026-06-01: 2026-06-01a —  Assembler −2·K offset bug (document_assembler.dart:238) is LIVE for docs WITHOUT server chunk_positions (positionsById == null). The fallback chunk textOffset used docCharCursor, which drops the inter-chunk “\n\n” +2 per chunk → highlight drifts −2·K, growing each chunk. Not all docs have server offsets — the meal-plan reproducer did not. Fix C-3 uses the separator-inclusive buffer position for the fallback.
- 2026-06-01: 2026-06-01b —  Instrumented diagnosis: a temporary debugPrint probe (recorded vs actual buffer offset + delta) decisively separated “assembler fallback bug” from “server-convention bug” from “cause elsewhere.” Probe stays in until the fix is verified (delta=0), then stripped. Prevented shipping a fix to a possibly-dormant path.
- 2026-06-01: 2026-06-01c —  Flutter TextSpan.backgroundColor paints from a glyph’s advance origin, not its visual ink extent. When the highlighted span is the FIRST child of a paragraph (headings/paragraphs, but not list items), a heavy bold cap’s leading bearing can sit outside the rect — looks like the first char is unhighlighted. (Theory; full-char magnitude + last-letter variant unexplained — verify visually before fixing.)
- 2026-06-01: 2026-06-01d —  Visual verification for CC: golden tests (flutter test + matchesGoldenFile) render widgets to PNGs CC can open — same Skia pipeline, faithful for paint artifacts IF real fonts are loaded (FontLoader); else Ahem placeholder hides the artifact. integration_test+screenshot or PowerShell window capture are higher-fidelity fallbacks. Never flutter clean.
- 2026-06-01: 2026-06-01e —  _jumpToDocumentOffset: autoPlay means “preserve current play/pause state across seek.” Added forcePlay (“play regardless”) for the line-play icon and line-text tap so a paused user gets playback. TOC/Contents jumps keep default forcePlay=false to preserve state.
- 2026-06-01: 2026-06-01f —  DOCX renders one DocumentReadingView per page (visibleBlocks = page.blocks); onActiveSentenceChanged is gated on the active block being visible, so at a page boundary the leaving page suppresses the callback and the entering page fires it ~1 frame later via addPostFrameCallback → ~1-frame tracker lag (cosmetic, not the highlight cause).
- 2026-06-01: 2026-06-01g —  Perf flag: the offset probe showed assemble() runs repeatedly (re-assembling the whole doc on many ticks/rebuilds). Not a correctness issue; investigate for performance later.
- 2026-06-01: Dated entries; never overwrite — always append.
- 2026-06-01: 2026-06-01 — Editor find was NOT green-field: a reading-mode find bar already existed (DOCX block-based + PDF PdfTextSearcher), and Ctrl+F in the player is captured by a global HardwareKeyboard handler, not the shell shortcut map. Lesson: read the surface before assuming its shape.
- 2026-06-01: 2026-06-01 — flutter_quill updateSelection only triggers bring-into-view on a drag or a focused text-input change; a programmatic selection on an UNFOCUSED editor reveals nothing (raw_editor_state gating). Edit-mode find scroll must compute geometry and drive the scroll controller itself.
- 2026-06-01: 2026-06-01 — The unified QuillEditor is scrollable:true with its own internal controller, but it’s embedded in the outer _contentScrollController which is what actually moves content on screen (confirmed by spike: hasClients=true, max>0, animateTo moves the page). Reveal a match via editorKey.currentState.renderEditor.getLocalRectForCaret → localToGlobal → drive the outer controller.
- 2026-06-01: 2026-06-01 — A selection-only updateSelection produces identical block dicts, so _handleDocxUnifiedChanged computes changed=false → no false “unsaved” flag. (This also explains why the earlier doc-reload change-event flood was harmless to the unsaved state.)
- 2026-06-01: 2026-06-01 — pdfrx PdfTextSearcher.goToNextMatch/goToPrevMatch update currentIndex and call invalidate() but do NOT notifyListeners(). The client must await + setState on nav, or the UI only refreshes when something else (e.g. the playback loop) rebuilds.
- 2026-06-01: 2026-06-01 — DocxPlayerNavigator renders in edit mode (unconditional). _activeReadingPageNumber drives BOTH the thumbnail highlight (isActive comparison) and the rail auto-scroll (didUpdateWidget → ensureVisible) — feed that one signal and both behaviors fire.
- 2026-06-01: 2026-06-01 — Icons.match_case does not exist in Flutter 3.24.5 (added later) — use a plain “Aa” glyph for the case-sensitivity toggle.
- 2026-06-01: 2026-06-01 — Pre-existing wart (logged, not fixed): loading a DIFFERENT DOCX while in edit mode re-hydrates the Quill controller (hundreds of change events), which falsely flags unsaved and triggers an auto-save. Surfaced by a probe; content stays intact.
- 2026-06-02: flutter_quill 10.8.5: QuillEditor.basic runs the config through copyWith, which forwards customStyleBuilder but drops onTapUp, onTapDown, and onSingleLongTap. Config-level tap hooks revert to null at runtime and analyze cannot catch it. Use a passive Listener plus renderEditor for tap detection.
- 2026-06-02: RenderEditor.getPositionForOffset expects a global offset and does globalToLocal internally (the Flutter RenderEditable convention). Pass the global pointer position directly; pre-converting double-counts and misplaces the offset.
- 2026-06-02: showMenu positioning: the RelativeRect anchor must be in the same coordinate space as its container. A global anchor with an Offset.zero by overlay.size container drifts by the overlay's offset. Convert the anchor to overlay-local first.
- 2026-06-02: Reading-view alignment (and similar) can round-trip through data correctly yet render inert if the text widget does not fill its block width. Check the render width, not just the data, when display and data disagree.
- 2026-06-02: KYCShield: Cloudflare Pages builds from source (public/index.html) and serves build/. With the Git integration disconnected, npx wrangler pages deploy build with the production branch is the reliable manual deploy.
- 2026-06-03: 2026-06-03b. Migrations do NOT auto-run on ECS startup; the image boots straight into uvicorn. They apply only via an explicit ECS one-off task running alembic upgrade head. Always ground-truth the prod revision with a read-only alembic current one-off before applying, rather than trusting an older assumption.
- 2026-06-03: 2026-06-03b. Blueprint is a pure additive overlay. Documents stay the single source of truth and are never duplicated or moved. The four new tables touch nothing on documents or projects and are fully reversible by dropping them. documents.project_id remains the only project-membership truth.
- 2026-06-03: 2026-06-03b. For a new owned table, prefer a UUID FK to users(id) over copying the projects TEXT-without-FK pattern. It gives referential integrity and makes the security-critical assignment isolation check a direct UUID comparison with no join.
- 2026-06-03: 2026-06-03b. A composite FK (parent_part_id, blueprint_id) referencing (id, blueprint_id) enforces a same-blueprint parent at the database level without a trigger.
- 2026-06-03: 2026-06-03b. Use TEXT plus CHECK for evolving controlled lists (genre, status, role), not native ENUM, to avoid the ALTER TYPE ADD VALUE in-transaction limitation and keep the lists easy to change.
- 2026-06-03: 2026-06-03b. Keep progress and readiness derived on read, never stored, so Blueprint owns structure only and adds no content-derived sync axis.
- 2026-06-03: 2026-06-03b. There is no local Docker on this machine; the cloud CI is the test gate. CI and Release are independent workflows, so a red CI does not block the Release deploy. A chronically red CI is therefore noise and must be fixed (M11) before Phase 2 so it can catch regressions in the new backend code.
- 2026-06-03: 2026-06-03b. NUMERIC gapped, midpoint ordering lets a drag-reorder write a single row, with no unique constraint forcing neighbor rewrites.
- 2026-06-08: CI was never an enforced gate and its integration test database was unwired. The app and Alembic read POSTGRES_*, but CI set DATABASE_*; with Settings ignoring extras, those values were silently dropped and tests fell back to wrong defaults, so they never connected. The integration step also ran no migration, so the schema was empty. Fix: set POSTGRES_* in the test-backend job and add an alembic upgrade head step before the tests.
- 2026-06-08: Alembic env.py applied the whole chain in one transaction. A fresh base-to-head apply fails on the enum migration because PostgreSQL forbids using a newly added enum value in the same transaction that added it. Fix: transaction_per_migration=True. Production was never affected because it applied migrations incrementally across separate ECS one-off runs, committing the enum value before any later use.
- 2026-06-08: Data-backfill migrations that target specific production records (migration 030, Stage 3 email backfill and Auth0 orphan purge) abort on any database lacking those records, which breaks fresh applies in CI, new environments, and disaster recovery. Principle: data migrations must no-op gracefully when their target data is absent and perform the backfill only when it is present.

## Last Devlog
- **File**: `C:\Users\Admin\OneDrive\_Psitta\Docs\DevLogs\Psitta_DevLog_20260608_CIIntegrationDBWiring_MigrationChainFix_v1_0.docx`
- **Recent commits** (`git log --oneline -10`):

```
f70fe63 fix(blueprints): drop ORM FKs to unmapped tables (users/projects/documents), DB constraints retained - fixes NoReferencedTableError on Blueprint flush
02d948a feat(blueprints): 2C write API - create, clone, update, delete with system-write guards and tests
863bc75 test(integration): bind get_db_session to a NullPool engine disposed in the test loop, fixing Event loop is closed
3a91511 feat(blueprints): 2B read API - schemas, service, GET /blueprints endpoints, tests
cf535b3 fix(migrations): no-op stage2 backfill on fresh/empty DB
d3984f4 fix(migrations): apply each migration in its own transaction
781d5bd ci(test-backend): wire integration DB via POSTGRES_* env + alembic upgrade head
e19845b test(M11): commit voice redirect fix and audit_log quarantine; scope coverage floor to unit step
8eb3f35 test(M11): fix integration auth and redirect handling (follow_redirects, opt-in auth override and JWKS stub)
de65c97 ci(M11): set backend coverage floor to 30 percent (honest current baseline, ratchet up later)
```
- _Auto-updated by Stop hook at 2026-06-08 12:59 UTC_

## Further Reading
- `ARCHITECTURE.md` — full system design and component diagram
- `OPEN_CORE_BOUNDARY.md` — what goes in core vs extensions
- `docs/adr/` — architecture decision records
- `docs/TESTING.md` — test strategy
- `CONTRIBUTING.md` — contributor workflow
