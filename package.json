{
  "name": "psitta-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Psitta — document-to-audio platform. Monorepo root.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "website:dev": "pnpm --filter website dev",
    "website:build": "pnpm --filter website build",
    "website:lint": "pnpm --filter website lint"
  }
}
