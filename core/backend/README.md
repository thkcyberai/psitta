# Psitta Backend

FastAPI backend service for Psitta — ultra-natural document narration.

## Quick Start
```bash
uvicorn psitta.main:create_app --factory --reload --host 0.0.0.0 --port 8000
```

See [docs/API.md](../../docs/API.md) for the full API specification.
